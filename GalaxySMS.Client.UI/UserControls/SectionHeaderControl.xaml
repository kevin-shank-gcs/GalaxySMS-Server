<UserControl x:Name="userControl" x:Class="GalaxySMS.Client.UI.SectionHeaderControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:sharedResources="clr-namespace:GalaxySMS.Resources;assembly=GalaxySMS.Resources" 
             mc:Ignorable="d" 
             d:DesignHeight="38.31" d:DesignWidth="600">

    <DockPanel HorizontalAlignment="Stretch" Height="Auto">
        <Border Style="{StaticResource HeaderBorder}">
            <StackPanel Orientation="Horizontal">
                <TextBlock Style="{StaticResource Heading2Left}" 
                           Text="{Binding Path=HeaderText, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControl}}}"
                           Margin="10" />
            </StackPanel>
        </Border>
    </DockPanel>
</UserControl>
