<ResourceDictionary
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation"
        xmlns:dataPager="clr-namespace:Telerik.Windows.Controls.Data.DataPager;assembly=Telerik.Windows.Controls.Data"
        xmlns:c="http://schemas.telerik.com/2008/xaml/compile"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="c"
        xmlns:telerik1="clr-namespace:Telerik.Windows.Controls;assembly=Telerik.Windows.Controls"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        xmlns:controls="clr-namespace:Telerik.Windows.Controls;assembly=Telerik.Windows.Controls.Data"
        xmlns:telerikData="clr-namespace:Telerik.Windows.Data;assembly=Telerik.Windows.Data"
        xmlns:dataFilter="clr-namespace:Telerik.Windows.Controls.Data.DataFilter;assembly=Telerik.Windows.Controls.Data"
        xmlns:dataForm="clr-namespace:Telerik.Windows.Controls.Data.DataForm;assembly=Telerik.Windows.Controls.Data"
        xmlns:collectionNavigator="clr-namespace:Telerik.Windows.Controls.Data.CollectionNavigator;assembly=Telerik.Windows.Controls.Data"
        xmlns:propertyGrid="clr-namespace:Telerik.Windows.Controls.Data.PropertyGrid;assembly=Telerik.Windows.Controls.Data">
    <ResourceDictionary.MergedDictionaries>
        <telerik:Office2016TouchResourceDictionary/>
    </ResourceDictionary.MergedDictionaries>
    <telerik1:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    <FontFamily x:Key="TelerikWebUI">/Telerik.Windows.Controls;component/Themes/Fonts/TelerikWebUI.ttf#TelerikWebUI</FontFamily>
    <sys:String x:Key="GlyphArrow60Up"></sys:String>
    <sys:String x:Key="GlyphArrow60Right"></sys:String>
    <sys:String x:Key="GlyphArrow60Down"></sys:String>
    <sys:String x:Key="GlyphArrow60Left"></sys:String>
    <sys:String x:Key="GlyphArrowForward"></sys:String>
    <sys:String x:Key="GlyphArrowBackward"></sys:String>
    <sys:String x:Key="GlyphArrowChevronRight"></sys:String>
    <sys:String x:Key="GlyphArrowChevronLeft"></sys:String>
    <sys:String x:Key="GlyphCategorize"></sys:String>
    <sys:String x:Key="GlyphEdit"></sys:String>
    <sys:String x:Key="GlyphDelete"></sys:String>
    <sys:String x:Key="GlyphClose"></sys:String>
    <sys:String x:Key="GlyphPlus"></sys:String>
    <sys:String x:Key="GlyphMinus"></sys:String>
    <sys:String x:Key="GlyphZoom"></sys:String>
    <sys:String x:Key="GlyphQuestion"></sys:String>
    <sys:String x:Key="GlyphFile"></sys:String>
    <ControlTemplate x:Key="ValidationTooltipTemplate">
        <StackPanel Orientation="Horizontal">
            <Grid SnapsToDevicePixels="True" VerticalAlignment="Top">
                <Border Background="Transparent" HorizontalAlignment="Right" VerticalAlignment="Top" Width="3" Height="3"/>
                <AdornedElementPlaceholder x:Name="Holder"/>
                <Border BorderBrush="{telerik1:Office2016TouchResource ResourceKey=ValidationBrush}" BorderThickness="1" CornerRadius="{telerik1:Office2016TouchResource ResourceKey=CornerRadius}"/>
            </Grid>
            <Grid>
                <Border Background="{telerik1:Office2016TouchResource ResourceKey=ValidationBrush}" Margin="5 0 0 0" MinHeight="32">
                    <TextBlock x:Name="PART_ToolTip"
                            FontSize="{telerik1:Office2016TouchResource ResourceKey=FontSize}"
                            FontFamily="{telerik1:Office2016TouchResource ResourceKey=FontFamily}"
                            Foreground="{telerik1:Office2016TouchResource ResourceKey=MarkerInvertedBrush}"
                            VerticalAlignment="Center"
                            Padding="4 6 4 5"
                            Text="{Binding ElementName=Holder, Path=AdornedElement.(Validation.Errors).CurrentItem.ErrorContent}"/>
                </Border>
            </Grid>
        </StackPanel>
    </ControlTemplate>
    <dataFilter:LogicalOperatorToCheckedConverter x:Key="LogicalOperatorToCheckedConverter"/>
    <dataFilter:FilterMemberToEnabledConverter x:Key="FilterMemberToEnabledConverter"/>
    <dataFilter:FilterOperatorToSelectedItemConverter x:Key="FilterOperatorToSelectedItemConverter"/>
    <collectionNavigator:CommandsButtonsVisibilityConverter x:Key="VisibilityConverter"/>
    <telerik:InvertedBooleanToVisibilityConverter x:Key="InvertedBooleanToVisibilityConverter"/>
    <telerikData:FilterCompositionLogicalOperatorConverter x:Key="LogicalOperatorConverter"/>
    <telerikData:FilterOperatorConverter x:Key="FilterOperatorConverter"/>
    <dataForm:DescriptionVisibilityConverter x:Key="DescriptionVisibilityConverter"/>
    <propertyGrid:FlatItemSourceConverter x:Key="FlatItemSourceConverter"/>
    <propertyGrid:HeaderVisibilityConverter x:Key="HeaderVisibilityConverter"/>
    <Style x:Key="TextBoxStyle" TargetType="TextBox">
        <Setter Property="Background" Value="{telerik1:Office2016TouchResource ResourceKey=MainBrush}"/>
        <Setter Property="BorderBrush" Value="{telerik1:Office2016TouchResource ResourceKey=BasicBrush}"/>
        <Setter Property="Foreground" Value="{telerik1:Office2016TouchResource ResourceKey=MarkerBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="4 6 4 5"/>
        <Setter Property="FontFamily" Value="{telerik1:Office2016TouchResource ResourceKey=FontFamily}"/>
        <Setter Property="FontSize" Value="{telerik1:Office2016TouchResource ResourceKey=FontSize}"/>
        <Setter Property="CaretBrush" Value="{telerik1:Office2016TouchResource ResourceKey=MarkerBrush}"/>
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="Validation.ErrorTemplate" Value="{StaticResource ValidationTooltipTemplate}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <Grid x:Name="root">
                        <Border x:Name="BorderVisual"
                                Background="{TemplateBinding Background}"
                                CornerRadius="{telerik1:Office2016TouchResource ResourceKey=CornerRadius}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"/>
                        <Border x:Name="ReadOnlyVisual"
                                BorderBrush="{telerik1:Office2016TouchResource ResourceKey=ReadOnlyBorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Background="{telerik1:Office2016TouchResource ResourceKey=ReadOnlyBackgroundBrush}"
                                CornerRadius="{telerik1:Office2016TouchResource ResourceKey=CornerRadius}"
                                Opacity="0"/>
                        <ScrollViewer x:Name="PART_ContentHost"
                                Margin="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}"
                                Foreground="{TemplateBinding Foreground}"
                                BorderBrush="Transparent"
                                BorderThickness="0"
                                Background="Transparent"
                                VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"
                                HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"/>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="BorderVisual" Property="BorderBrush" Value="{telerik1:Office2016TouchResource ResourceKey=AccentMouseOverBrush}"/>
                        </Trigger>
                        <Trigger Property="IsReadOnly" Value="True">
                            <Setter TargetName="ReadOnlyVisual" Property="Opacity" Value="1"/>
                            <Setter TargetName="BorderVisual" Property="BorderBrush" Value="{telerik1:Office2016TouchResource ResourceKey=ReadOnlyBorderBrush}"/>
                            <Setter TargetName="BorderVisual" Property="Background" Value="{telerik1:Office2016TouchResource ResourceKey=ReadOnlyBackgroundBrush}"/>
                        </Trigger>
                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Setter TargetName="BorderVisual" Property="BorderBrush" Value="{telerik1:Office2016TouchResource ResourceKey=AccentFocusedBrush}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="PART_ContentHost" Property="Opacity" Value="{telerik1:Office2016TouchResource ResourceKey=DisabledOpacity}"/>
                            <Setter Property="Background" Value="{telerik1:Office2016TouchResource ResourceKey=PrimaryBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style TargetType="TextBox" BasedOn="{StaticResource TextBoxStyle}"/>
    <Style x:Key="RadButtonStyle" TargetType="telerik1:RadButton">
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="FontFamily" Value="{telerik1:Office2016TouchResource ResourceKey=FontFamily}"/>
        <Setter Property="FontSize" Value="{telerik1:Office2016TouchResource ResourceKey=FontSize}"/>
        <Setter Property="Foreground" Value="{telerik1:Office2016TouchResource ResourceKey=MarkerBrush}"/>
        <Setter Property="Background" Value="{telerik1:Office2016TouchResource ResourceKey=MainBrush}"/>
        <Setter Property="BorderBrush" Value="{telerik1:Office2016TouchResource ResourceKey=BasicBrush}"/>
        <Setter Property="Padding" Value="4 6 4 5"/>
        <Setter Property="UseLayoutRounding" Value="True"/>
        <Setter Property="CornerRadius" Value="{telerik1:Office2016TouchResource ResourceKey=CornerRadius}"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="telerik1:RadButton">
                    <Grid>
                        <Border x:Name="BorderVisual"
                                SnapsToDevicePixels="True"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                Background="{TemplateBinding Background}"
                                CornerRadius="{TemplateBinding CornerRadius}">
                            <ContentPresenter x:Name="Content"
                                    TextBlock.Foreground="{TemplateBinding Foreground}"
                                    Margin="{TemplateBinding Padding}"
                                    Content="{TemplateBinding Content}"
                                    ContentTemplate="{TemplateBinding ContentTemplate}"
                                    VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                    HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                    ContentTemplateSelector="{TemplateBinding ContentTemplateSelector}"
                                    ContentStringFormat="{TemplateBinding ContentStringFormat}"
                                    RecognizesAccessKey="True"/>
                        </Border>
                        <Border x:Name="FocusVisual"
                                Opacity="0"
                                BorderThickness="{telerik1:Office2016TouchResource ResourceKey=FocusThickness}"
                                BorderBrush="{telerik1:Office2016TouchResource ResourceKey=AccentFocusedBrush}"
                                SnapsToDevicePixels="True"
                                CornerRadius="{TemplateBinding CornerRadius}"/>
                    </Grid>
                    <ControlTemplate.Triggers>

                        <!-- background is hidden -->
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsEnabled" Value="True"/>
                                <Condition Property="IsBackgroundVisible" Value="False"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="BorderVisual" Property="Background" Value="Transparent"/>
                            <Setter TargetName="BorderVisual" Property="BorderBrush" Value="Transparent"/>
                        </MultiTrigger>
                        <Trigger Property="IsFocused" Value="True">
                            <Setter TargetName="FocusVisual" Property="Opacity" Value="1"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="BorderVisual" Property="Background" Value="{telerik1:Office2016TouchResource ResourceKey=AccentMouseOverBrush}"/>
                            <Setter TargetName="BorderVisual" Property="BorderBrush" Value="{telerik1:Office2016TouchResource ResourceKey=AccentMouseOverBrush}"/>
                            <Setter TargetName="Content" Property="TextElement.Foreground" Value="{telerik1:Office2016TouchResource ResourceKey=MarkerInvertedBrush}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="BorderVisual" Property="Background" Value="{telerik1:Office2016TouchResource ResourceKey=AccentPressedBrush}"/>
                            <Setter TargetName="BorderVisual" Property="BorderBrush" Value="{telerik1:Office2016TouchResource ResourceKey=AccentPressedBrush}"/>
                            <Setter TargetName="Content" Property="TextElement.Foreground" Value="{telerik1:Office2016TouchResource ResourceKey=MarkerInvertedBrush}"/>
                        </Trigger>

                        <!-- hovered and focused -->
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsMouseOver" Value="True"/>
                                <Condition Property="IsFocused" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="FocusVisual" Property="Opacity" Value="0"/>
                        </MultiTrigger>

                        <!-- pressed and focused -->
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsFocused" Value="True"/>
                                <Condition Property="IsPressed" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="FocusVisual" Property="Opacity" Value="0"/>
                        </MultiTrigger>

                        <!-- disabled -->
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsEnabled" Value="False"/>
                                <Condition Property="IsBackgroundVisible" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="Content" Property="Opacity" Value="{telerik1:Office2016TouchResource ResourceKey=DisabledOpacity}"/>
                            <Setter TargetName="BorderVisual" Property="Background" Value="{telerik1:Office2016TouchResource ResourceKey=PrimaryBrush}"/>
                            <Setter TargetName="BorderVisual" Property="BorderBrush" Value="{telerik1:Office2016TouchResource ResourceKey=BasicBrush}"/>
                        </MultiTrigger>

                        <!-- disabled and focused -->
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsEnabled" Value="False"/>
                                <Condition Property="IsFocused" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="FocusVisual" Property="Opacity" Value="0"/>
                        </MultiTrigger>

                        <!-- disabled & background is hidden -->
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsEnabled" Value="False"/>
                                <Condition Property="IsBackgroundVisible" Value="False"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="Content" Property="Opacity" Value="{telerik1:Office2016TouchResource ResourceKey=DisabledOpacity}"/>
                            <Setter TargetName="BorderVisual" Property="Background" Value="Transparent"/>
                            <Setter TargetName="BorderVisual" Property="BorderBrush" Value="Transparent"/>
                        </MultiTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style x:Key="RadPathButtonPathStyle" TargetType="Path">
        <Setter Property="Fill" Value="{telerik1:Office2016TouchResource ResourceKey=IconBrush}"/>
        <Setter Property="Stretch" Value="Fill"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
    </Style>
    <Style x:Key="RadPathButtonStyle" TargetType="telerik1:RadPathButton">
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="FontFamily" Value="{telerik1:Office2016TouchResource ResourceKey=FontFamily}"/>
        <Setter Property="FontSize" Value="{telerik1:Office2016TouchResource ResourceKey=FontSize}"/>
        <Setter Property="Foreground" Value="{telerik1:Office2016TouchResource ResourceKey=MarkerBrush}"/>
        <Setter Property="Background" Value="{telerik1:Office2016TouchResource ResourceKey=MainBrush}"/>
        <Setter Property="BorderBrush" Value="{telerik1:Office2016TouchResource ResourceKey=BasicBrush}"/>
        <Setter Property="Padding" Value="4"/>
        <Setter Property="CornerRadius" Value="{telerik1:Office2016TouchResource ResourceKey=CornerRadius}"/>
        <Setter Property="PathStyle" Value="{StaticResource RadPathButtonPathStyle}"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="telerik1:RadPathButton">
                    <Grid>
                        <Border x:Name="BorderVisual"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                Background="{TemplateBinding Background}"
                                CornerRadius="{TemplateBinding CornerRadius}"
                                SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}">
                            <Grid Margin="{TemplateBinding Padding}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <Path x:Name="Path" Grid.Column="1" Grid.Row="1" Style="{TemplateBinding PathStyle}" Data="{TemplateBinding PathGeometry}"/>
                                <Grid x:Name="ContentHost" Grid.Column="1" Grid.Row="2" Margin="0 3 0 0">
                                    <ContentPresenter x:Name="Content"
                                            TextBlock.Foreground="{TemplateBinding Foreground}"
                                            Margin="{TemplateBinding ContentMargin}"
                                            Content="{TemplateBinding Content}"
                                            ContentTemplate="{TemplateBinding ContentTemplate}"
                                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                            ContentTemplateSelector="{TemplateBinding ContentTemplateSelector}"
                                            ContentStringFormat="{TemplateBinding ContentStringFormat}"
                                            RecognizesAccessKey="True"/>
                                </Grid>
                            </Grid>
                        </Border>
                        <Border x:Name="FocusVisual"
                                Opacity="0"
                                BorderThickness="{telerik1:Office2016TouchResource ResourceKey=FocusThickness}"
                                BorderBrush="{telerik1:Office2016TouchResource ResourceKey=AccentFocusedBrush}"
                                SnapsToDevicePixels="True"
                                CornerRadius="{TemplateBinding CornerRadius}"/>
                    </Grid>
                    <ControlTemplate.Triggers>

                        <!-- background is hidden -->
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsEnabled" Value="True"/>
                                <Condition Property="IsBackgroundVisible" Value="False"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="BorderVisual" Property="Background" Value="Transparent"/>
                            <Setter TargetName="BorderVisual" Property="BorderBrush" Value="Transparent"/>
                        </MultiTrigger>
                        <Trigger Property="IsFocused" Value="True">
                            <Setter TargetName="FocusVisual" Property="Opacity" Value="1"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="true">
                            <Setter TargetName="BorderVisual" Property="Background" Value="{telerik1:Office2016TouchResource ResourceKey=AccentMouseOverBrush}"/>
                            <Setter TargetName="BorderVisual" Property="BorderBrush" Value="{telerik1:Office2016TouchResource ResourceKey=AccentMouseOverBrush}"/>
                            <Setter TargetName="Path" Property="Fill" Value="{telerik1:Office2016TouchResource ResourceKey=MarkerInvertedBrush}"/>
                            <Setter TargetName="Content" Property="TextElement.Foreground" Value="{telerik1:Office2016TouchResource ResourceKey=MarkerInvertedBrush}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="BorderVisual" Property="Background" Value="{telerik1:Office2016TouchResource ResourceKey=AccentPressedBrush}"/>
                            <Setter TargetName="BorderVisual" Property="BorderBrush" Value="{telerik1:Office2016TouchResource ResourceKey=AccentPressedBrush}"/>
                            <Setter TargetName="Path" Property="Fill" Value="{telerik1:Office2016TouchResource ResourceKey=MarkerInvertedBrush}"/>
                            <Setter TargetName="Content" Property="TextElement.Foreground" Value="{telerik1:Office2016TouchResource ResourceKey=MarkerInvertedBrush}"/>
                        </Trigger>

                        <!-- hovered and focused -->
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsMouseOver" Value="True"/>
                                <Condition Property="IsFocused" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="FocusVisual" Property="Opacity" Value="0"/>
                        </MultiTrigger>

                        <!-- pressed and focused -->
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsFocused" Value="True"/>
                                <Condition Property="IsPressed" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="FocusVisual" Property="Opacity" Value="0"/>
                        </MultiTrigger>

                        <!-- disabled -->
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsEnabled" Value="False"/>
                                <Condition Property="IsBackgroundVisible" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="Content" Property="Opacity" Value="{telerik1:Office2016TouchResource ResourceKey=DisabledOpacity}"/>
                            <Setter TargetName="Path" Property="Opacity" Value="{telerik1:Office2016TouchResource ResourceKey=DisabledOpacity}"/>
                            <Setter TargetName="Path" Property="Fill" Value="{telerik1:Office2016TouchResource ResourceKey=IconBrush}"/>
                            <Setter TargetName="BorderVisual" Property="BorderBrush" Value="{telerik1:Office2016TouchResource ResourceKey=BasicBrush}"/>
                            <Setter TargetName="BorderVisual" Property="Background" Value="{telerik1:Office2016TouchResource ResourceKey=PrimaryBrush}"/>
                        </MultiTrigger>

                        <!-- disabled & background is hidden -->
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsEnabled" Value="False"/>
                                <Condition Property="IsBackgroundVisible" Value="False"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="Content" Property="Opacity" Value="{telerik1:Office2016TouchResource ResourceKey=DisabledOpacity}"/>
                            <Setter TargetName="Path" Property="Opacity" Value="{telerik1:Office2016TouchResource ResourceKey=DisabledOpacity}"/>
                            <Setter TargetName="Path" Property="Fill" Value="{telerik1:Office2016TouchResource ResourceKey=IconBrush}"/>
                            <Setter TargetName="BorderVisual" Property="Background" Value="Transparent"/>
                            <Setter TargetName="BorderVisual" Property="BorderBrush" Value="Transparent"/>
                        </MultiTrigger>

                        <!-- content & content template are not preset -->
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="Content" Value="{x:Null}"/>
                                <Condition Property="ContentTemplate" Value="{x:Null}"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="ContentHost" Property="Visibility" Value="Collapsed"/>
                        </MultiTrigger>

                        <!-- content placement -->
                        <Trigger Property="ContentPlacement" Value="Left">
                            <Setter TargetName="ContentHost" Property="Grid.Column" Value="0"/>
                            <Setter TargetName="ContentHost" Property="Grid.Row" Value="1"/>
                            <Setter TargetName="ContentHost" Property="Margin" Value="0 0 3 0"/>
                        </Trigger>
                        <Trigger Property="ContentPlacement" Value="Right">
                            <Setter TargetName="ContentHost" Property="Grid.Column" Value="2"/>
                            <Setter TargetName="ContentHost" Property="Grid.Row" Value="1"/>
                            <Setter TargetName="ContentHost" Property="Margin" Value="3 0 0 0"/>
                        </Trigger>
                        <Trigger Property="ContentPlacement" Value="Top">
                            <Setter TargetName="ContentHost" Property="Grid.Column" Value="1"/>
                            <Setter TargetName="ContentHost" Property="Grid.Row" Value="0"/>
                            <Setter TargetName="ContentHost" Property="Margin" Value="0 0 0 3"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style x:Key="RadToggleButtonStyle" TargetType="telerik1:RadToggleButton">
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="FontFamily" Value="{telerik1:Office2016TouchResource ResourceKey=FontFamily}"/>
        <Setter Property="FontSize" Value="{telerik1:Office2016TouchResource ResourceKey=FontSize}"/>
        <Setter Property="Foreground" Value="{telerik1:Office2016TouchResource ResourceKey=MarkerBrush}"/>
        <Setter Property="Background" Value="{telerik1:Office2016TouchResource ResourceKey=MainBrush}"/>
        <Setter Property="BorderBrush" Value="{telerik1:Office2016TouchResource ResourceKey=BasicBrush}"/>
        <Setter Property="Padding" Value="4 6 4 5"/>
        <Setter Property="UseLayoutRounding" Value="True"/>
        <Setter Property="CornerRadius" Value="{telerik1:Office2016TouchResource ResourceKey=CornerRadius}"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="telerik1:RadToggleButton">
                    <Grid>
                        <Border
                                SnapsToDevicePixels="True"
                                x:Name="BorderVisual"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                Background="{TemplateBinding Background}"
                                CornerRadius="{TemplateBinding CornerRadius}">
                            <ContentPresenter x:Name="Content"
                                    Margin="{TemplateBinding Padding}"
                                    Content="{TemplateBinding Content}"
                                    ContentTemplate="{TemplateBinding ContentTemplate}"
                                    VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                    HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                    ContentTemplateSelector="{TemplateBinding ContentTemplateSelector}"
                                    ContentStringFormat="{TemplateBinding ContentStringFormat}"
                                    RecognizesAccessKey="True"/>
                        </Border>
                        <Border x:Name="FocusVisual"
                                Opacity="0"
                                BorderThickness="{telerik1:Office2016TouchResource ResourceKey=FocusThickness}"
                                BorderBrush="{telerik1:Office2016TouchResource ResourceKey=AccentFocusedBrush}"
                                SnapsToDevicePixels="True"
                                CornerRadius="{TemplateBinding CornerRadius}"/>
                    </Grid>
                    <ControlTemplate.Triggers>

                        <!-- background is hidden -->
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsEnabled" Value="True"/>
                                <Condition Property="IsBackgroundVisible" Value="False"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="BorderVisual" Property="Background" Value="Transparent"/>
                            <Setter TargetName="BorderVisual" Property="BorderBrush" Value="Transparent"/>
                        </MultiTrigger>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="BorderVisual" Property="Background" Value="{telerik1:Office2016TouchResource ResourceKey=AccentBrush}"/>
                            <Setter TargetName="BorderVisual" Property="BorderBrush" Value="{telerik1:Office2016TouchResource ResourceKey=AccentBrush}"/>
                            <Setter TargetName="Content" Property="TextElement.Foreground" Value="{telerik1:Office2016TouchResource ResourceKey=MarkerInvertedBrush}"/>
                        </Trigger>
                        <Trigger Property="IsFocused" Value="True">
                            <Setter TargetName="FocusVisual" Property="Opacity" Value="1"/>
                        </Trigger>

                        <!-- checked & focused -->
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsFocused" Value="True"/>
                                <Condition Property="IsChecked" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="FocusVisual" Property="Opacity" Value="1"/>
                            <Setter TargetName="FocusVisual" Property="Margin" Value="1"/>
                        </MultiTrigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="FocusVisual" Property="Opacity" Value="0"/>
                            <Setter TargetName="BorderVisual" Property="Background" Value="{telerik1:Office2016TouchResource ResourceKey=AccentMouseOverBrush}"/>
                            <Setter TargetName="BorderVisual" Property="BorderBrush" Value="{telerik1:Office2016TouchResource ResourceKey=AccentMouseOverBrush}"/>
                            <Setter TargetName="Content" Property="TextElement.Foreground" Value="{telerik1:Office2016TouchResource ResourceKey=MarkerInvertedBrush}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="FocusVisual" Property="Opacity" Value="0"/>
                            <Setter TargetName="BorderVisual" Property="Background" Value="{telerik1:Office2016TouchResource ResourceKey=AccentPressedBrush}"/>
                            <Setter TargetName="BorderVisual" Property="BorderBrush" Value="{telerik1:Office2016TouchResource ResourceKey=AccentPressedBrush}"/>
                            <Setter TargetName="Content" Property="TextElement.Foreground" Value="{telerik1:Office2016TouchResource ResourceKey=MarkerInvertedBrush}"/>
                        </Trigger>

                        <!-- disabled -->
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsEnabled" Value="False"/>
                                <Condition Property="IsBackgroundVisible" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="Content" Property="Opacity" Value="{telerik1:Office2016TouchResource ResourceKey=DisabledOpacity}"/>
                            <Setter TargetName="BorderVisual" Property="Background" Value="{telerik1:Office2016TouchResource ResourceKey=PrimaryBrush}"/>
                            <Setter TargetName="BorderVisual" Property="BorderBrush" Value="{telerik1:Office2016TouchResource ResourceKey=BasicBrush}"/>
                        </MultiTrigger>

                        <!-- disabled and focused -->
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsEnabled" Value="False"/>
                                <Condition Property="IsFocused" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="FocusVisual" Property="Opacity" Value="0"/>
                        </MultiTrigger>

                        <!-- disabled & background is hidden -->
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsEnabled" Value="False"/>
                                <Condition Property="IsBackgroundVisible" Value="False"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="Content" Property="Opacity" Value="{telerik1:Office2016TouchResource ResourceKey=DisabledOpacity}"/>
                            <Setter TargetName="BorderVisual" Property="Background" Value="Transparent"/>
                            <Setter TargetName="BorderVisual" Property="BorderBrush" Value="Transparent"/>
                        </MultiTrigger>

                        <!-- disabled & checked -->
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsEnabled" Value="False"/>
                                <Condition Property="IsChecked" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="Content" Property="Opacity" Value="{telerik1:Office2016TouchResource ResourceKey=DisabledOpacity}"/>
                            <Setter TargetName="BorderVisual" Property="Background" Value="{telerik1:Office2016TouchResource ResourceKey=AccentBrush}"/>
                            <Setter TargetName="BorderVisual" Property="BorderBrush" Value="{telerik1:Office2016TouchResource ResourceKey=AccentBrush}"/>
                        </MultiTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style x:Key="RadRadioButtonStyle" TargetType="telerik1:RadRadioButton">
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="FontFamily" Value="{telerik1:Office2016TouchResource ResourceKey=FontFamily}"/>
        <Setter Property="FontSize" Value="{telerik1:Office2016TouchResource ResourceKey=FontSize}"/>
        <Setter Property="Foreground" Value="{telerik1:Office2016TouchResource ResourceKey=MarkerBrush}"/>
        <Setter Property="Background" Value="{telerik1:Office2016TouchResource ResourceKey=MainBrush}"/>
        <Setter Property="BorderBrush" Value="{telerik1:Office2016TouchResource ResourceKey=BasicBrush}"/>
        <Setter Property="Padding" Value="4 6 4 5"/>
        <Setter Property="UseLayoutRounding" Value="True"/>
        <Setter Property="CornerRadius" Value="{telerik1:Office2016TouchResource ResourceKey=CornerRadius}"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="telerik1:RadRadioButton">
                    <Grid>
                        <Border
                                SnapsToDevicePixels="True"
                                x:Name="BorderVisual"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                Background="{TemplateBinding Background}"
                                CornerRadius="{TemplateBinding CornerRadius}">
                            <ContentPresenter x:Name="Content"
                                    Margin="{TemplateBinding Padding}"
                                    Content="{TemplateBinding Content}"
                                    ContentTemplate="{TemplateBinding ContentTemplate}"
                                    VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                    HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                    ContentTemplateSelector="{TemplateBinding ContentTemplateSelector}"
                                    ContentStringFormat="{TemplateBinding ContentStringFormat}"
                                    RecognizesAccessKey="True"/>
                        </Border>
                        <Border x:Name="FocusVisual"
                                Opacity="0"
                                BorderThickness="{telerik1:Office2016TouchResource ResourceKey=FocusThickness}"
                                BorderBrush="{telerik1:Office2016TouchResource ResourceKey=AccentFocusedBrush}"
                                SnapsToDevicePixels="True"
                                CornerRadius="{TemplateBinding CornerRadius}"/>
                    </Grid>
                    <ControlTemplate.Triggers>

                        <!-- background is hidden -->
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsEnabled" Value="True"/>
                                <Condition Property="IsBackgroundVisible" Value="False"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="BorderVisual" Property="Background" Value="Transparent"/>
                            <Setter TargetName="BorderVisual" Property="BorderBrush" Value="Transparent"/>
                        </MultiTrigger>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="BorderVisual" Property="Background" Value="{telerik1:Office2016TouchResource ResourceKey=AccentBrush}"/>
                            <Setter TargetName="BorderVisual" Property="BorderBrush" Value="{telerik1:Office2016TouchResource ResourceKey=AccentBrush}"/>
                            <Setter TargetName="Content" Property="TextElement.Foreground" Value="{telerik1:Office2016TouchResource ResourceKey=MarkerInvertedBrush}"/>
                        </Trigger>
                        <Trigger Property="IsFocused" Value="True">
                            <Setter TargetName="FocusVisual" Property="Opacity" Value="1"/>
                        </Trigger>

                        <!-- checked & focused -->
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsFocused" Value="True"/>
                                <Condition Property="IsChecked" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="FocusVisual" Property="Opacity" Value="1"/>
                            <Setter TargetName="FocusVisual" Property="Margin" Value="1"/>
                        </MultiTrigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="FocusVisual" Property="Opacity" Value="0"/>
                            <Setter TargetName="BorderVisual" Property="Background" Value="{telerik1:Office2016TouchResource ResourceKey=AccentMouseOverBrush}"/>
                            <Setter TargetName="BorderVisual" Property="BorderBrush" Value="{telerik1:Office2016TouchResource ResourceKey=AccentMouseOverBrush}"/>
                            <Setter TargetName="Content" Property="TextElement.Foreground" Value="{telerik1:Office2016TouchResource ResourceKey=MarkerInvertedBrush}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="FocusVisual" Property="Opacity" Value="0"/>
                            <Setter TargetName="BorderVisual" Property="Background" Value="{telerik1:Office2016TouchResource ResourceKey=AccentPressedBrush}"/>
                            <Setter TargetName="BorderVisual" Property="BorderBrush" Value="{telerik1:Office2016TouchResource ResourceKey=AccentPressedBrush}"/>
                            <Setter TargetName="Content" Property="TextElement.Foreground" Value="{telerik1:Office2016TouchResource ResourceKey=MarkerInvertedBrush}"/>
                        </Trigger>

                        <!-- disabled -->
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsEnabled" Value="False"/>
                                <Condition Property="IsBackgroundVisible" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="Content" Property="Opacity" Value="{telerik1:Office2016TouchResource ResourceKey=DisabledOpacity}"/>
                            <Setter TargetName="BorderVisual" Property="Background" Value="{telerik1:Office2016TouchResource ResourceKey=PrimaryBrush}"/>
                            <Setter TargetName="BorderVisual" Property="BorderBrush" Value="{telerik1:Office2016TouchResource ResourceKey=BasicBrush}"/>
                        </MultiTrigger>

                        <!-- disabled and focused -->
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsEnabled" Value="False"/>
                                <Condition Property="IsFocused" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="FocusVisual" Property="Opacity" Value="0"/>
                        </MultiTrigger>

                        <!-- disabled & background is hidden -->
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsEnabled" Value="False"/>
                                <Condition Property="IsBackgroundVisible" Value="False"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="Content" Property="Opacity" Value="{telerik1:Office2016TouchResource ResourceKey=DisabledOpacity}"/>
                            <Setter TargetName="BorderVisual" Property="Background" Value="Transparent"/>
                            <Setter TargetName="BorderVisual" Property="BorderBrush" Value="Transparent"/>
                        </MultiTrigger>

                        <!-- disabled & checked -->
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsEnabled" Value="False"/>
                                <Condition Property="IsChecked" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="Content" Property="Opacity" Value="{telerik1:Office2016TouchResource ResourceKey=DisabledOpacity}"/>
                            <Setter TargetName="BorderVisual" Property="Background" Value="{telerik1:Office2016TouchResource ResourceKey=AccentBrush}"/>
                            <Setter TargetName="BorderVisual" Property="BorderBrush" Value="{telerik1:Office2016TouchResource ResourceKey=AccentBrush}"/>
                        </MultiTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style x:Key="RadDropDownButtonStyle" TargetType="telerik1:RadDropDownButton">
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="FontFamily" Value="{telerik1:Office2016TouchResource ResourceKey=FontFamily}"/>
        <Setter Property="FontSize" Value="{telerik1:Office2016TouchResource ResourceKey=FontSize}"/>
        <Setter Property="Foreground" Value="{telerik1:Office2016TouchResource ResourceKey=MarkerBrush}"/>
        <Setter Property="Background" Value="{telerik1:Office2016TouchResource ResourceKey=MainBrush}"/>
        <Setter Property="BorderBrush" Value="{telerik1:Office2016TouchResource ResourceKey=BasicBrush}"/>
        <Setter Property="Padding" Value="4 6 4 5"/>
        <Setter Property="UseLayoutRounding" Value="True"/>
        <Setter Property="CornerRadius" Value="{telerik1:Office2016TouchResource ResourceKey=CornerRadius}"/>
        <Setter Property="IsOpen" Value="false"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="telerik1:RadDropDownButton">
                    <Grid SnapsToDevicePixels="True">
                        <Border x:Name="BorderVisual"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                Background="{TemplateBinding Background}"
                                CornerRadius="{TemplateBinding CornerRadius}">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <ContentPresenter x:Name="Content"
                                        Grid.Row="1"
                                        Grid.Column="1"
                                        TextBlock.Foreground="{TemplateBinding Foreground}"
                                        Margin="{TemplateBinding Padding}"
                                        Content="{TemplateBinding Content}"
                                        ContentTemplate="{TemplateBinding ContentTemplate}"
                                        VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                        HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                        ContentTemplateSelector="{TemplateBinding ContentTemplateSelector}"
                                        ContentStringFormat="{TemplateBinding ContentStringFormat}"
                                        RecognizesAccessKey="True"/>
                                <Grid x:Name="DropDownIndicator"
                                        IsHitTestVisible="False"
                                        Grid.Column="1"
                                        Grid.Row="2"
                                        HorizontalAlignment="Center"
                                        Canvas.ZIndex="2"
                                        VerticalAlignment="Center"
                                        Visibility="{TemplateBinding DropDownIndicatorVisibility}"
                                        Margin="7">
                                    <TextBlock x:Name="arrow"
                                            Text="{StaticResource GlyphArrow60Down}"
                                            Foreground="{telerik1:Office2016TouchResource ResourceKey=IconBrush}"
                                            FontFamily="{StaticResource TelerikWebUI}"
                                            FontWeight="Normal"
                                            FontStyle="Normal"
                                            FontSize="16"/>
                                </Grid>
                            </Grid>
                        </Border>
                        <Border x:Name="FocusVisual"
                                Opacity="0"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                BorderBrush="{telerik1:Office2016TouchResource ResourceKey=AccentFocusedBrush}"
                                SnapsToDevicePixels="True"
                                CornerRadius="{TemplateBinding CornerRadius}"/>
                        <Popup x:Name="DropDownPopup"
                                AllowsTransparency="True"
                                StaysOpen="False"
                                Focusable="False"
                                Placement="Bottom"
                                PopupAnimation="{TemplateBinding PopupAnimation}"
                                HorizontalOffset="0"
                                VerticalOffset="1">
                            <Border x:Name="DropDownPopupBorder"
                                    BorderThickness="1"
                                    MinWidth="3"
                                    MinHeight="3"
                                    Width="{TemplateBinding DropDownWidth}"
                                    Height="{TemplateBinding DropDownHeight}"
                                    MaxWidth="{TemplateBinding DropDownMaxWidth}"
                                    MaxHeight="{TemplateBinding DropDownMaxHeight}"
                                    CornerRadius="{telerik1:Office2016TouchResource ResourceKey=CornerRadius}"
                                    Background="{telerik1:Office2016TouchResource ResourceKey=MainBrush}"
                                    BorderBrush="{telerik1:Office2016TouchResource ResourceKey=BasicBrush}">
                                <ContentControl x:Name="DropDownPopupContent"
                                        Content="{TemplateBinding DropDownContent}"
                                        HorizontalContentAlignment="Stretch"
                                        VerticalContentAlignment="Stretch"
                                        ContentTemplate="{TemplateBinding DropDownContentTemplate}"
                                        HorizontalAlignment="Stretch"
                                        VerticalAlignment="Stretch"
                                        DataContext="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=DataContext}"
                                        ContentStringFormat="{TemplateBinding ContentStringFormat}"
                                        ContentTemplateSelector="{TemplateBinding ContentTemplateSelector}"/>
                            </Border>
                        </Popup>
                    </Grid>
                    <ControlTemplate.Triggers>

                        <!-- background is hidden -->
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsEnabled" Value="True"/>
                                <Condition Property="IsBackgroundVisible" Value="False"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="BorderVisual" Property="Background" Value="Transparent"/>
                            <Setter TargetName="BorderVisual" Property="BorderBrush" Value="Transparent"/>
                        </MultiTrigger>
                        <Trigger Property="IsFocused" Value="True">
                            <Setter TargetName="FocusVisual" Property="Opacity" Value="1"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="FocusVisual" Property="Opacity" Value="0"/>
                            <Setter TargetName="BorderVisual" Property="Background" Value="{telerik1:Office2016TouchResource ResourceKey=AccentMouseOverBrush}"/>
                            <Setter TargetName="BorderVisual" Property="BorderBrush" Value="{telerik1:Office2016TouchResource ResourceKey=AccentMouseOverBrush}"/>
                            <Setter TargetName="Content" Property="TextElement.Foreground" Value="{telerik1:Office2016TouchResource ResourceKey=MarkerInvertedBrush}"/>
                            <Setter TargetName="arrow" Property="Foreground" Value="{telerik1:Office2016TouchResource ResourceKey=MarkerInvertedBrush}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="FocusVisual" Property="Opacity" Value="0"/>
                            <Setter TargetName="BorderVisual" Property="Background" Value="{telerik1:Office2016TouchResource ResourceKey=AccentPressedBrush}"/>
                            <Setter TargetName="BorderVisual" Property="BorderBrush" Value="{telerik1:Office2016TouchResource ResourceKey=AccentPressedBrush}"/>
                            <Setter TargetName="Content" Property="TextElement.Foreground" Value="{telerik1:Office2016TouchResource ResourceKey=MarkerInvertedBrush}"/>
                            <Setter TargetName="arrow" Property="Foreground" Value="{telerik1:Office2016TouchResource ResourceKey=MarkerInvertedBrush}"/>
                        </Trigger>
                        <Trigger Property="IsOpen" Value="True">
                            <Setter TargetName="arrow" Property="Foreground" Value="{telerik1:Office2016TouchResource ResourceKey=MarkerInvertedBrush}"/>
                            <Setter TargetName="BorderVisual" Property="BorderBrush" Value="{telerik1:Office2016TouchResource ResourceKey=AccentPressedBrush}"/>
                            <Setter TargetName="BorderVisual" Property="Background" Value="{telerik1:Office2016TouchResource ResourceKey=AccentPressedBrush}"/>
                        </Trigger>

                        <!-- disabled -->
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsEnabled" Value="False"/>
                                <Condition Property="IsBackgroundVisible" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="Content" Property="Opacity" Value="{telerik1:Office2016TouchResource ResourceKey=DisabledOpacity}"/>
                            <Setter TargetName="arrow" Property="Opacity" Value="{telerik1:Office2016TouchResource ResourceKey=DisabledOpacity}"/>
                            <Setter TargetName="BorderVisual" Property="Background" Value="{telerik1:Office2016TouchResource ResourceKey=PrimaryBrush}"/>
                            <Setter TargetName="BorderVisual" Property="BorderBrush" Value="{telerik1:Office2016TouchResource ResourceKey=BasicBrush}"/>
                        </MultiTrigger>

                        <!-- disabled and focused -->
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsEnabled" Value="False"/>
                                <Condition Property="IsFocused" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="FocusVisual" Property="Opacity" Value="0"/>
                        </MultiTrigger>

                        <!-- disabled & background is hidden -->
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsEnabled" Value="False"/>
                                <Condition Property="IsBackgroundVisible" Value="False"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="Content" Property="Opacity" Value="{telerik1:Office2016TouchResource ResourceKey=DisabledOpacity}"/>
                            <Setter TargetName="arrow" Property="Opacity" Value="{telerik1:Office2016TouchResource ResourceKey=DisabledOpacity}"/>
                            <Setter TargetName="BorderVisual" Property="Background" Value="Transparent"/>
                            <Setter TargetName="BorderVisual" Property="BorderBrush" Value="Transparent"/>
                        </MultiTrigger>
                        <Trigger Property="DropDownButtonPosition" Value="Bottom"/>
                        <Trigger Property="DropDownButtonPosition" Value="Left">
                            <Setter TargetName="DropDownIndicator" Property="Grid.Column" Value="0"/>
                            <Setter TargetName="DropDownIndicator" Property="Grid.Row" Value="1"/>
                        </Trigger>
                        <Trigger Property="DropDownButtonPosition" Value="Top">
                            <Setter TargetName="DropDownIndicator" Property="Grid.Column" Value="1"/>
                            <Setter TargetName="DropDownIndicator" Property="Grid.Row" Value="0"/>
                        </Trigger>
                        <Trigger Property="DropDownButtonPosition" Value="Right">
                            <Setter TargetName="DropDownIndicator" Property="Grid.Column" Value="2"/>
                            <Setter TargetName="DropDownIndicator" Property="Grid.Row" Value="1"/>
                        </Trigger>
                        <Trigger Property="DropDownPlacement" Value="Bottom"/>
                        <Trigger Property="DropDownPlacement" Value="Top">
                            <Setter TargetName="DropDownPopup" Property="HorizontalOffset" Value="0"/>
                            <Setter TargetName="DropDownPopup" Property="VerticalOffset" Value="-1"/>
                            <Setter TargetName="arrow" Property="TextBlock.Text" Value="{StaticResource GlyphArrow60Up}"/>
                        </Trigger>
                        <Trigger Property="DropDownPlacement" Value="Left">
                            <Setter TargetName="DropDownPopup" Property="HorizontalOffset" Value="-1"/>
                            <Setter TargetName="DropDownPopup" Property="VerticalOffset" Value="0"/>
                            <Setter TargetName="arrow" Property="TextBlock.Text" Value="{StaticResource GlyphArrow60Left}"/>
                        </Trigger>
                        <Trigger Property="DropDownPlacement" Value="Right">
                            <Setter TargetName="DropDownPopup" Property="HorizontalOffset" Value="1"/>
                            <Setter TargetName="DropDownPopup" Property="VerticalOffset" Value="0"/>
                            <Setter TargetName="arrow" Property="TextBlock.Text" Value="{StaticResource GlyphArrow60Right}"/>
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="FlowDirection" Value="RightToLeft"/>
                                <Condition Property="DropDownPlacement" Value="Left"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="arrow" Property="TextBlock.Text" Value="{StaticResource GlyphArrow60Right}"/>
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="FlowDirection" Value="RightToLeft"/>
                                <Condition Property="DropDownPlacement" Value="Right"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="arrow" Property="TextBlock.Text" Value="{StaticResource GlyphArrow60Left}"/>
                        </MultiTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style x:Key="SplitButtonPartStyle" TargetType="telerik1:RadButton" BasedOn="{StaticResource RadButtonStyle}">
        <Setter Property="IsTabStop" Value="False"/>
        <Setter Property="Visibility" Value="{Binding RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay, Path=IsButtonPartVisible, Converter={StaticResource BooleanToVisibilityConverter}}"/>
        <Setter Property="Background" Value="{Binding Background, RelativeSource={RelativeSource TemplatedParent}}"/>
        <Setter Property="BorderThickness" Value="{Binding BorderThickness, RelativeSource={RelativeSource TemplatedParent}}"/>
        <Setter Property="Padding" Value="{Binding Padding, RelativeSource={RelativeSource TemplatedParent}}"/>
        <Setter Property="IsBackgroundVisible" Value="{Binding IsBackgroundVisible, RelativeSource={RelativeSource TemplatedParent}}"/>
        <Setter Property="Foreground" Value="{Binding Foreground, RelativeSource={RelativeSource TemplatedParent}}"/>
        <Setter Property="FontSize" Value="{Binding FontSize, RelativeSource={RelativeSource TemplatedParent}}"/>
        <Setter Property="HorizontalContentAlignment" Value="{Binding HorizontalContentAlignment, RelativeSource={RelativeSource TemplatedParent}}"/>
        <Setter Property="VerticalContentAlignment" Value="{Binding VerticalContentAlignment, RelativeSource={RelativeSource TemplatedParent}}"/>
        <Setter Property="MinHeight" Value="{Binding MinHeight, RelativeSource={RelativeSource TemplatedParent}}"/>
        <Setter Property="Focusable" Value="False"/>
    </Style>
    <Style x:Key="SplitToggleButtonStyle" TargetType="telerik1:RadToggleButton" BasedOn="{StaticResource RadToggleButtonStyle}">
        <Setter Property="Margin" Value="-1 0 0 0"/>
        <Setter Property="BorderThickness" Value="{Binding BorderThickness, RelativeSource={RelativeSource TemplatedParent}}"/>
        <Setter Property="BorderBrush" Value="{Binding BorderBrush, RelativeSource={RelativeSource TemplatedParent}}"/>
        <Setter Property="Background" Value="{Binding Background, RelativeSource={RelativeSource TemplatedParent}}"/>
        <Setter Property="FontSize" Value="{Binding FontSize, RelativeSource={RelativeSource TemplatedParent}}"/>
        <Setter Property="Foreground" Value="{telerik1:Office2016TouchResource ResourceKey=IconBrush}"/>
        <Setter Property="Padding" Value="7"/>
        <Setter Property="Focusable" Value="False"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="telerik1:RadToggleButton">
                    <Border
                            SnapsToDevicePixels="True"
                            x:Name="BorderVisual"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            Background="{TemplateBinding Background}"
                            CornerRadius="{TemplateBinding CornerRadius}">
                        <ContentPresenter x:Name="Content"
                                Margin="{TemplateBinding Padding}"
                                Content="{TemplateBinding Content}"
                                ContentTemplate="{TemplateBinding ContentTemplate}"
                                VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                ContentTemplateSelector="{TemplateBinding ContentTemplateSelector}"
                                ContentStringFormat="{TemplateBinding ContentStringFormat}"
                                RecognizesAccessKey="True"/>
                    </Border>
                    <ControlTemplate.Triggers>

                        <!-- background is hidden -->
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsEnabled" Value="True"/>
                                <Condition Property="IsChecked" Value="False"/>
                                <Condition Property="IsBackgroundVisible" Value="False"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="BorderVisual" Property="Background" Value="Transparent"/>
                            <Setter TargetName="BorderVisual" Property="BorderBrush" Value="Transparent"/>
                        </MultiTrigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="BorderVisual" Property="Background" Value="{telerik1:Office2016TouchResource ResourceKey=AccentMouseOverBrush}"/>
                            <Setter TargetName="BorderVisual" Property="BorderBrush" Value="{telerik1:Office2016TouchResource ResourceKey=AccentMouseOverBrush}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="BorderVisual" Property="Background" Value="{telerik1:Office2016TouchResource ResourceKey=AccentPressedBrush}"/>
                            <Setter TargetName="BorderVisual" Property="BorderBrush" Value="{telerik1:Office2016TouchResource ResourceKey=AccentPressedBrush}"/>
                        </Trigger>

                        <!-- disabled -->
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsEnabled" Value="False"/>
                                <Condition Property="IsBackgroundVisible" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="Content" Property="Opacity" Value="{telerik1:Office2016TouchResource ResourceKey=DisabledOpacity}"/>
                            <Setter TargetName="BorderVisual" Property="Background" Value="{telerik1:Office2016TouchResource ResourceKey=PrimaryBrush}"/>
                            <Setter TargetName="BorderVisual" Property="BorderBrush" Value="{telerik1:Office2016TouchResource ResourceKey=BasicBrush}"/>
                        </MultiTrigger>

                        <!-- disabled & background is hidden -->
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsEnabled" Value="False"/>
                                <Condition Property="IsBackgroundVisible" Value="False"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="Content" Property="Opacity" Value="{telerik1:Office2016TouchResource ResourceKey=DisabledOpacity}"/>
                            <Setter TargetName="BorderVisual" Property="Background" Value="Transparent"/>
                            <Setter TargetName="BorderVisual" Property="BorderBrush" Value="Transparent"/>
                        </MultiTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style x:Key="RadSplitButtonStyle" TargetType="telerik1:RadSplitButton">
        <Setter Property="ButtonPartStyle" Value="{StaticResource SplitButtonPartStyle}"/>
        <Setter Property="TogglePartStyle" Value="{StaticResource SplitToggleButtonStyle}"/>
        <Setter Property="FontFamily" Value="{telerik1:Office2016TouchResource ResourceKey=FontFamily}"/>
        <Setter Property="FontSize" Value="{telerik1:Office2016TouchResource ResourceKey=FontSize}"/>
        <Setter Property="Foreground" Value="{telerik1:Office2016TouchResource ResourceKey=MarkerBrush}"/>
        <Setter Property="Background" Value="{telerik1:Office2016TouchResource ResourceKey=MainBrush}"/>
        <Setter Property="BorderBrush" Value="{telerik1:Office2016TouchResource ResourceKey=BasicBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="4 6 4 5"/>
        <Setter Property="UseLayoutRounding" Value="True"/>
        <Setter Property="CornerRadius" Value="{telerik1:Office2016TouchResource ResourceKey=CornerRadius}"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="IsTabStop" Value="true"/>
        <Setter Property="IsOpen" Value="false"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="telerik1:RadSplitButton">
                    <Grid SnapsToDevicePixels="True">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <telerik1:RadButton x:Name="ButtonPart"
                                    Style="{TemplateBinding ButtonPartStyle}"
                                    Grid.Row="1"
                                    Grid.Column="1"
                                    ContentStringFormat="{TemplateBinding ContentStringFormat}"
                                    ContentTemplateSelector="{TemplateBinding ContentTemplateSelector}"
                                    Content="{TemplateBinding Content}"
                                    ContentTemplate="{TemplateBinding ContentTemplate}"/>
                            <telerik1:RadToggleButton x:Name="DropDownPart"
                                    IsChecked="{Binding Path=IsOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                    Style="{TemplateBinding TogglePartStyle}"
                                    Grid.Row="1"
                                    Grid.Column="2"
                                    IsBackgroundVisible="{TemplateBinding IsBackgroundVisible}"
                                    Visibility="{TemplateBinding DropDownIndicatorVisibility}">
                                <telerik1:RadToggleButton.Content>
                                    <TextBlock x:Name="arrow"
                                            Text="{StaticResource GlyphArrow60Down}"
                                            FontWeight="Normal"
                                            FontStyle="Normal"
                                            Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=telerik1:RadToggleButton}}"
                                            FontFamily="{StaticResource TelerikWebUI}"
                                            FontSize="16"/>
                                </telerik1:RadToggleButton.Content>
                            </telerik1:RadToggleButton>
                        </Grid>
                        <Border x:Name="BorderVisual"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                CornerRadius="{TemplateBinding CornerRadius}"
                                Background="{x:Null}"
                                SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"
                                IsHitTestVisible="False"/>
                        <Border x:Name="FocusVisual"
                                Opacity="0"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                BorderBrush="{telerik1:Office2016TouchResource ResourceKey=AccentFocusedBrush}"
                                SnapsToDevicePixels="True"
                                CornerRadius="{TemplateBinding CornerRadius}"/>
                        <Popup x:Name="DropDownPopup"
                                AllowsTransparency="True"
                                StaysOpen="False"
                                Focusable="False"
                                Placement="Bottom"
                                PopupAnimation="{TemplateBinding PopupAnimation}"
                                HorizontalOffset="0"
                                VerticalOffset="1">
                            <Border x:Name="DropDownPopupBorder"
                                    BorderThickness="1"
                                    MinWidth="3"
                                    MinHeight="3"
                                    Width="{TemplateBinding DropDownWidth}"
                                    Height="{TemplateBinding DropDownHeight}"
                                    MaxWidth="{TemplateBinding DropDownMaxWidth}"
                                    MaxHeight="{TemplateBinding DropDownMaxHeight}"
                                    Background="{telerik1:Office2016TouchResource ResourceKey=MainBrush}"
                                    BorderBrush="{telerik1:Office2016TouchResource ResourceKey=BasicBrush}"
                                    CornerRadius="{telerik1:Office2016TouchResource ResourceKey=CornerRadius}">
                                <ContentControl x:Name="DropDownPopupContent"
                                        Content="{TemplateBinding DropDownContent}"
                                        ContentTemplate="{TemplateBinding DropDownContentTemplate}"
                                        HorizontalContentAlignment="Stretch"
                                        VerticalContentAlignment="Stretch"
                                        HorizontalAlignment="Stretch"
                                        VerticalAlignment="Stretch"
                                        DataContext="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=DataContext}"/>
                            </Border>
                        </Popup>
                    </Grid>
                    <ControlTemplate.Triggers>

                        <!-- background is hidden -->
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsEnabled" Value="True"/>
                                <Condition Property="IsOpen" Value="False"/>
                                <Condition Property="IsBackgroundVisible" Value="False"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="BorderVisual" Property="BorderBrush" Value="Transparent"/>
                            <Setter TargetName="DropDownPart" Property="Background" Value="Transparent"/>
                            <Setter TargetName="DropDownPart" Property="BorderBrush" Value="Transparent"/>
                        </MultiTrigger>

                        <!-- focused -->
                        <Trigger Property="IsFocused" Value="True">
                            <Setter TargetName="FocusVisual" Property="Opacity" Value="1"/>
                        </Trigger>

                        <!-- mouse over & opened -->
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="BorderVisual" Property="BorderBrush" Value="{telerik1:Office2016TouchResource ResourceKey=AccentMouseOverBrush}"/>
                            <Setter TargetName="DropDownPart" Property="BorderBrush" Value="{telerik1:Office2016TouchResource ResourceKey=AccentMouseOverBrush}"/>
                            <Setter TargetName="FocusVisual" Property="Opacity" Value="0"/>
                        </Trigger>

                        <!-- disabled -->
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsEnabled" Value="False"/>
                                <Condition Property="IsBackgroundVisible" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="DropDownPart" Property="Foreground" Value="{telerik1:Office2016TouchResource ResourceKey=MarkerBrush}"/>
                        </MultiTrigger>

                        <!-- disabled and focused -->
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsEnabled" Value="False"/>
                                <Condition Property="IsFocused" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="FocusVisual" Property="Opacity" Value="0"/>
                        </MultiTrigger>

                        <!-- disabled & background is hidden -->
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsEnabled" Value="False"/>
                                <Condition Property="IsBackgroundVisible" Value="False"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="DropDownPart" Property="Foreground" Value="{telerik1:Office2016TouchResource ResourceKey=MarkerBrush}"/>
                            <Setter TargetName="BorderVisual" Property="BorderBrush" Value="Transparent"/>
                            <Setter TargetName="DropDownPart" Property="Background" Value="Transparent"/>
                            <Setter TargetName="DropDownPart" Property="BorderBrush" Value="Transparent"/>
                        </MultiTrigger>

                        <!-- enabled & opened -->
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsEnabled" Value="True"/>
                                <Condition Property="IsOpen" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="ButtonPart" Property="Background" Value="{telerik1:Office2016TouchResource ResourceKey=AccentPressedBrush}"/>
                            <Setter TargetName="ButtonPart" Property="BorderBrush" Value="{telerik1:Office2016TouchResource ResourceKey=AccentPressedBrush}"/>
                            <Setter TargetName="ButtonPart" Property="Foreground" Value="{telerik1:Office2016TouchResource ResourceKey=MarkerInvertedBrush}"/>
                            <Setter TargetName="DropDownPart" Property="Foreground" Value="{telerik1:Office2016TouchResource ResourceKey=MarkerInvertedBrush}"/>
                            <Setter TargetName="DropDownPart" Property="Background" Value="{telerik1:Office2016TouchResource ResourceKey=AccentPressedBrush}"/>
                            <Setter TargetName="DropDownPart" Property="BorderBrush" Value="{telerik1:Office2016TouchResource ResourceKey=AccentPressedBrush}"/>
                        </MultiTrigger>

                        <!-- DropDownButton Position -->
                        <Trigger Property="DropDownButtonPosition" Value="Bottom">
                            <Setter TargetName="DropDownPart" Property="Grid.Column" Value="1"/>
                            <Setter TargetName="DropDownPart" Property="Grid.Row" Value="2"/>
                            <Setter TargetName="DropDownPart" Property="Margin" Value="0 -1 0 0"/>
                            <Setter TargetName="DropDownPart" Property="CornerRadius" Value="{telerik1:Office2016TouchResource ResourceKey=CornerRadiusBottom}"/>
                            <Setter TargetName="ButtonPart" Property="CornerRadius" Value="{telerik1:Office2016TouchResource ResourceKey=CornerRadiusTop}"/>
                        </Trigger>
                        <Trigger Property="DropDownButtonPosition" Value="Left">
                            <Setter TargetName="DropDownPart" Property="Grid.Column" Value="0"/>
                            <Setter TargetName="DropDownPart" Property="Grid.Row" Value="1"/>
                            <Setter TargetName="DropDownPart" Property="Margin" Value="0 0 -1 0"/>
                            <Setter TargetName="DropDownPart" Property="CornerRadius" Value="{telerik1:Office2016TouchResource ResourceKey=CornerRadiusLeft}"/>
                            <Setter TargetName="ButtonPart" Property="CornerRadius" Value="{telerik1:Office2016TouchResource ResourceKey=CornerRadiusRight}"/>
                        </Trigger>
                        <Trigger Property="DropDownButtonPosition" Value="Top">
                            <Setter TargetName="DropDownPart" Property="Grid.Column" Value="1"/>
                            <Setter TargetName="DropDownPart" Property="Grid.Row" Value="0"/>
                            <Setter TargetName="DropDownPart" Property="Margin" Value="0 0 0 -1"/>
                            <Setter TargetName="DropDownPart" Property="CornerRadius" Value="{telerik1:Office2016TouchResource ResourceKey=CornerRadiusTop}"/>
                            <Setter TargetName="ButtonPart" Property="CornerRadius" Value="{telerik1:Office2016TouchResource ResourceKey=CornerRadiusBottom}"/>
                        </Trigger>
                        <Trigger Property="DropDownButtonPosition" Value="Right">
                            <Setter TargetName="DropDownPart" Property="Grid.Column" Value="2"/>
                            <Setter TargetName="DropDownPart" Property="Grid.Row" Value="1"/>
                            <Setter TargetName="DropDownPart" Property="Margin" Value="-1 0 0 0"/>
                            <Setter TargetName="DropDownPart" Property="CornerRadius" Value="{telerik1:Office2016TouchResource ResourceKey=CornerRadiusRight}"/>
                            <Setter TargetName="ButtonPart" Property="CornerRadius" Value="{telerik1:Office2016TouchResource ResourceKey=CornerRadiusLeft}"/>
                        </Trigger>
                        <Trigger Property="DropDownPlacement" Value="Bottom"/>
                        <Trigger Property="DropDownPlacement" Value="Top">
                            <Setter TargetName="DropDownPopup" Property="HorizontalOffset" Value="0"/>
                            <Setter TargetName="DropDownPopup" Property="VerticalOffset" Value="-1"/>
                        </Trigger>
                        <Trigger Property="DropDownPlacement" Value="Left">
                            <Setter TargetName="DropDownPopup" Property="HorizontalOffset" Value="-1"/>
                            <Setter TargetName="DropDownPopup" Property="VerticalOffset" Value="0"/>
                        </Trigger>
                        <Trigger Property="DropDownPlacement" Value="Right">
                            <Setter TargetName="DropDownPopup" Property="HorizontalOffset" Value="1"/>
                            <Setter TargetName="DropDownPopup" Property="VerticalOffset" Value="0"/>
                        </Trigger>

                        <!-- ButtonPartVisibility - Hidden -->
                        <Trigger Property="IsButtonPartVisible" Value="False">
                            <Setter TargetName="DropDownPart" Property="Margin" Value="0"/>
                            <Setter TargetName="DropDownPart" Property="Grid.Row" Value="0"/>
                            <Setter TargetName="DropDownPart" Property="Grid.RowSpan" Value="3"/>
                            <Setter TargetName="DropDownPart" Property="Grid.Column" Value="0"/>
                            <Setter TargetName="DropDownPart" Property="Grid.ColumnSpan" Value="3"/>
                            <Setter TargetName="DropDownPart" Property="IsTabStop" Value="True"/>
                            <Setter TargetName="DropDownPart" Property="CornerRadius" Value="{telerik1:Office2016TouchResource ResourceKey=CornerRadius}"/>
                            <Setter TargetName="ButtonPart" Property="Visibility" Value="Collapsed"/>
                        </Trigger>

                        <!-- DropDownIndicatorVisibility - Hidden -->
                        <Trigger Property="DropDownIndicatorVisibility" Value="Collapsed">
                            <Setter TargetName="ButtonPart" Property="CornerRadius" Value="{telerik1:Office2016TouchResource ResourceKey=CornerRadius}"/>
                        </Trigger>
                        <Trigger Property="DropDownPlacement" Value="Bottom"/>
                        <Trigger Property="DropDownPlacement" Value="Top">
                            <Setter TargetName="DropDownPopup" Property="HorizontalOffset" Value="0"/>
                            <Setter TargetName="DropDownPopup" Property="VerticalOffset" Value="-1"/>
                            <Setter TargetName="arrow" Property="TextBlock.Text" Value="{StaticResource GlyphArrow60Up}"/>
                        </Trigger>
                        <Trigger Property="DropDownPlacement" Value="Left">
                            <Setter TargetName="DropDownPopup" Property="HorizontalOffset" Value="-1"/>
                            <Setter TargetName="DropDownPopup" Property="VerticalOffset" Value="0"/>
                            <Setter TargetName="arrow" Property="TextBlock.Text" Value="{StaticResource GlyphArrow60Left}"/>
                        </Trigger>
                        <Trigger Property="DropDownPlacement" Value="Right">
                            <Setter TargetName="DropDownPopup" Property="HorizontalOffset" Value="1"/>
                            <Setter TargetName="DropDownPopup" Property="VerticalOffset" Value="0"/>
                            <Setter TargetName="arrow" Property="TextBlock.Text" Value="{StaticResource GlyphArrow60Right}"/>
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="FlowDirection" Value="RightToLeft"/>
                                <Condition Property="DropDownPlacement" Value="Left"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="arrow" Property="TextBlock.Text" Value="{StaticResource GlyphArrow60Right}"/>
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="FlowDirection" Value="RightToLeft"/>
                                <Condition Property="DropDownPlacement" Value="Right"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="arrow" Property="TextBlock.Text" Value="{StaticResource GlyphArrow60Left}"/>
                        </MultiTrigger>
                        <DataTrigger Binding="{Binding ElementName=DropDownPart, Path=IsMouseOver}" Value="True">
                            <Setter TargetName="DropDownPart" Property="Foreground" Value="{telerik1:Office2016TouchResource ResourceKey=MarkerInvertedBrush}"/>
                        </DataTrigger>

                        <!-- IsPressed -->
                        <DataTrigger Binding="{Binding ElementName=ButtonPart, Path=IsPressed}" Value="True">
                            <Setter TargetName="BorderVisual" Property="BorderBrush" Value="{telerik1:Office2016TouchResource ResourceKey=AccentPressedBrush}"/>
                            <Setter TargetName="DropDownPart" Property="BorderBrush" Value="{telerik1:Office2016TouchResource ResourceKey=AccentPressedBrush}"/>
                            <Setter TargetName="FocusVisual" Property="Opacity" Value="0"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding ElementName=DropDownPart, Path=IsPressed}" Value="True">
                            <Setter TargetName="BorderVisual" Property="BorderBrush" Value="{telerik1:Office2016TouchResource ResourceKey=AccentPressedBrush}"/>
                            <Setter TargetName="DropDownPart" Property="Foreground" Value="{telerik1:Office2016TouchResource ResourceKey=SelectedBrush}"/>
                            <Setter TargetName="FocusVisual" Property="Opacity" Value="0"/>
                        </DataTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style TargetType="telerik1:RadButton" BasedOn="{StaticResource RadButtonStyle}"/>
    <Style TargetType="telerik1:RadPathButton" BasedOn="{StaticResource RadPathButtonStyle}"/>
    <Style TargetType="telerik1:RadToggleButton" BasedOn="{StaticResource RadToggleButtonStyle}"/>
    <Style TargetType="telerik1:RadRadioButton" BasedOn="{StaticResource RadRadioButtonStyle}"/>
    <Style TargetType="telerik1:RadDropDownButton" BasedOn="{StaticResource RadDropDownButtonStyle}"/>
    <Style TargetType="telerik1:RadSplitButton" BasedOn="{StaticResource RadSplitButtonStyle}"/>
    <Style TargetType="dataPager:DataPagerTextBox" BasedOn="{StaticResource TextBoxStyle}"/>

    <!-- PageButton Style -->
    <Style x:Key="PageButtonStyle" TargetType="telerik:RadButton">
        <Setter Property="FontFamily" Value="{telerik1:Office2016TouchResource ResourceKey=FontFamily}"/>
        <Setter Property="FontSize" Value="{telerik1:Office2016TouchResource ResourceKey=FontSize}"/>
        <Setter Property="Foreground" Value="{telerik1:Office2016TouchResource ResourceKey=IconBrush}"/>
        <Setter Property="Background" Value="{telerik1:Office2016TouchResource ResourceKey=MainBrush}"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="4"/>
        <Setter Property="Margin" Value="0"/>
        <Setter Property="MinWidth" Value="32"/>
        <Setter Property="MinHeight" Value="32"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="CornerRadius" Value="{telerik1:Office2016TouchResource ResourceKey=CornerRadius}"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="telerik:RadButton">
                    <Grid>
                        <Border x:Name="BorderVisual"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                Background="{TemplateBinding Background}"
                                CornerRadius="{TemplateBinding CornerRadius}"
                                SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}">
                            <ContentPresenter x:Name="Content"
                                    TextBlock.Foreground="{TemplateBinding Foreground}"
                                    Margin="{TemplateBinding Padding}"
                                    Content="{TemplateBinding Content}"
                                    ContentTemplate="{TemplateBinding ContentTemplate}"
                                    VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                    HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                    ContentTemplateSelector="{TemplateBinding ContentTemplateSelector}"
                                    ContentStringFormat="{TemplateBinding ContentStringFormat}"
                                    RecognizesAccessKey="True"/>
                        </Border>
                        <Border x:Name="FocusVisual"
                                Opacity="0"
                                BorderBrush="{telerik1:Office2016TouchResource ResourceKey=AccentFocusedBrush}"
                                BorderThickness="{telerik1:Office2016TouchResource ResourceKey=FocusThickness}"
                                CornerRadius="{TemplateBinding CornerRadius}"
                                SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsFocused" Value="True">
                            <Setter TargetName="FocusVisual" Property="Opacity" Value="1"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="BorderVisual" Property="Background" Value="{telerik1:Office2016TouchResource ResourceKey=MouseOverBrush}"/>
                            <Setter TargetName="BorderVisual" Property="BorderBrush" Value="{telerik1:Office2016TouchResource ResourceKey=MouseOverBrush}"/>
                            <Setter TargetName="FocusVisual" Property="Opacity" Value="0"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="BorderVisual" Property="Background" Value="{telerik1:Office2016TouchResource ResourceKey=PressedBrush}"/>
                            <Setter TargetName="BorderVisual" Property="BorderBrush" Value="{telerik1:Office2016TouchResource ResourceKey=PressedBrush}"/>
                            <Setter TargetName="FocusVisual" Property="Opacity" Value="0"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Content" Property="Opacity" Value="{telerik1:Office2016TouchResource ResourceKey=DisabledOpacity}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- PageStyle -->
    <Style x:Key="RadPageStyle" TargetType="telerik:RadRadioButton">
        <Setter Property="FontFamily" Value="{telerik1:Office2016TouchResource ResourceKey=FontFamily}"/>
        <Setter Property="FontSize" Value="{telerik1:Office2016TouchResource ResourceKey=FontSize}"/>
        <Setter Property="Foreground" Value="{telerik1:Office2016TouchResource ResourceKey=MarkerBrush}"/>
        <Setter Property="Background" Value="{telerik1:Office2016TouchResource ResourceKey=MainBrush}"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="4"/>
        <Setter Property="Margin" Value="0 0 4 0"/>
        <Setter Property="MinWidth" Value="32"/>
        <Setter Property="CornerRadius" Value="{telerik1:Office2016TouchResource ResourceKey=CornerRadius}"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="telerik:RadRadioButton">
                    <Grid>
                        <Border x:Name="BorderVisual"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="{TemplateBinding CornerRadius}"
                                SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}">
                            <ContentPresenter x:Name="Content"
                                    Margin="{TemplateBinding Padding}"
                                    Content="{TemplateBinding Content}"
                                    ContentTemplate="{TemplateBinding ContentTemplate}"
                                    VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                    HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                    ContentTemplateSelector="{TemplateBinding ContentTemplateSelector}"
                                    ContentStringFormat="{TemplateBinding ContentStringFormat}"
                                    RecognizesAccessKey="True"/>
                        </Border>
                        <Border x:Name="FocusVisual"
                                Opacity="0"
                                BorderBrush="{telerik1:Office2016TouchResource ResourceKey=AccentFocusedBrush}"
                                BorderThickness="{telerik1:Office2016TouchResource ResourceKey=FocusThickness}"
                                CornerRadius="{TemplateBinding CornerRadius}"
                                SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="BorderVisual" Property="Background" Value="{telerik1:Office2016TouchResource ResourceKey=SelectedBrush}"/>
                            <Setter TargetName="BorderVisual" Property="BorderBrush" Value="{telerik1:Office2016TouchResource ResourceKey=SelectedBrush}"/>
                        </Trigger>
                        <Trigger Property="IsFocused" Value="True">
                            <Setter TargetName="FocusVisual" Property="Opacity" Value="1"/>
                        </Trigger>

                        <!-- disabled checked -->
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsEnabled" Value="False"/>
                                <Condition Property="IsChecked" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="Content" Property="Opacity" Value="{telerik1:Office2016TouchResource ResourceKey=DisabledOpacity}"/>
                            <Setter TargetName="BorderVisual" Property="Background" Value="{telerik1:Office2016TouchResource ResourceKey=SelectedBrush}"/>
                            <Setter TargetName="BorderVisual" Property="BorderBrush" Value="{telerik1:Office2016TouchResource ResourceKey=SelectedBrush}"/>
                        </MultiTrigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="FocusVisual" Property="Opacity" Value="0"/>
                            <Setter TargetName="BorderVisual" Property="Background" Value="{telerik1:Office2016TouchResource ResourceKey=MouseOverBrush}"/>
                            <Setter TargetName="BorderVisual" Property="BorderBrush" Value="{telerik1:Office2016TouchResource ResourceKey=MouseOverBrush}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="FocusVisual" Property="Opacity" Value="0"/>
                            <Setter TargetName="BorderVisual" Property="Background" Value="{telerik1:Office2016TouchResource ResourceKey=PressedBrush}"/>
                            <Setter TargetName="BorderVisual" Property="BorderBrush" Value="{telerik1:Office2016TouchResource ResourceKey=PressedBrush}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Content" Property="Opacity" Value="{telerik1:Office2016TouchResource ResourceKey=DisabledOpacity}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <ControlTemplate x:Key="NumericElementsPresenterTemplate" TargetType="dataPager:NumericElementsPresenter">
        <ItemsControl ItemsSource="{Binding Pages}" SnapsToDevicePixels="True" FocusVisualStyle="{x:Null}" IsTabStop="False">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <VirtualizingStackPanel Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <telerik:RadRadioButton
                            IsChecked="{Binding IsCurrent, Mode=OneWay}"
                            GroupName="{Binding GroupName}"
                            CommandParameter="{Binding PageIndex}"
                            Command="{x:Static controls:RadDataPagerCommands.MoveToPage}"
                            Content="{Binding Content}"
                            Style="{Binding NumericButtonStyle}"/>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </ControlTemplate>
    <Style x:Key="NumericElementsPresenterStyle" TargetType="dataPager:NumericElementsPresenter">
        <Setter Property="Template" Value="{StaticResource NumericElementsPresenterTemplate}"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="IsTabStop" Value="False"/>
    </Style>
    <ControlTemplate x:Key="DataPagerPresenterTemplate" TargetType="dataPager:DataPagerPresenter">
        <Border
                Background="{TemplateBinding Background}"
                BorderBrush="{TemplateBinding BorderBrush}"
                BorderThickness="{TemplateBinding BorderThickness}"
                Padding="{TemplateBinding Padding}">
            <Grid Background="Transparent" VerticalAlignment="{TemplateBinding VerticalContentAlignment}" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel
                        Orientation="Horizontal"
                        Background="Transparent"
                        VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                        HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}">

                    <!-- FirstPage Button -->
                    <telerik:RadButton Name="MoveToFirstPageButton" Command="{x:Static controls:RadDataPagerCommands.MoveToFirstPage}" Visibility="{Binding PagerControlsVisibility.MoveToFirstPageControlVisibility,RelativeSource={RelativeSource TemplatedParent}}">
                        <telerik:RadButton.Style>
                            <Style TargetType="{x:Type telerik:RadButton}" BasedOn="{StaticResource PageButtonStyle}">
                                <Style.Triggers>
                                    <Trigger Property="FlowDirection" Value="LeftToRight">
                                        <Setter Property="Content">
                                            <Setter.Value>
                                                <TextBlock
                                                        Text="{StaticResource GlyphArrowBackward}"
                                                        FontFamily="{StaticResource TelerikWebUI}"
                                                        Foreground="{Binding (TextElement.Foreground), RelativeSource={RelativeSource AncestorType=ContentPresenter}}"
                                                        FontSize="16"
                                                        FontStyle="Normal"
                                                        FontWeight="Normal"
                                                        HorizontalAlignment="Center"
                                                        VerticalAlignment="Center"/>
                                            </Setter.Value>
                                        </Setter>
                                    </Trigger>
                                    <Trigger Property="FlowDirection" Value="RightToLeft">
                                        <Setter Property="Content">
                                            <Setter.Value>
                                                <TextBlock
                                                        Text="{StaticResource GlyphArrowForward}"
                                                        FontFamily="{StaticResource TelerikWebUI}"
                                                        Foreground="{Binding (TextElement.Foreground), RelativeSource={RelativeSource AncestorType=ContentPresenter}}"
                                                        FontSize="16"
                                                        FontStyle="Normal"
                                                        FontWeight="Normal"
                                                        HorizontalAlignment="Center"
                                                        VerticalAlignment="Center"/>
                                            </Setter.Value>
                                        </Setter>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </telerik:RadButton.Style>
                    </telerik:RadButton>

                    <!-- PreviousPage Button -->
                    <telerik:RadButton Name="MoveToPreviousPageButton" Command="{x:Static controls:RadDataPagerCommands.MoveToPreviousPage}" Visibility="{Binding PagerControlsVisibility.MoveToPreviousPageControlVisibility,RelativeSource={RelativeSource TemplatedParent}}">
                        <telerik:RadButton.Style>
                            <Style TargetType="{x:Type telerik:RadButton}" BasedOn="{StaticResource PageButtonStyle}">
                                <Style.Triggers>
                                    <Trigger Property="FlowDirection" Value="LeftToRight">
                                        <Setter Property="Content">
                                            <Setter.Value>
                                                <TextBlock
                                                        Text="{StaticResource GlyphArrow60Left}"
                                                        FontFamily="{StaticResource TelerikWebUI}"
                                                        Foreground="{Binding (TextElement.Foreground), RelativeSource={RelativeSource AncestorType=ContentPresenter}}"
                                                        FontSize="16"
                                                        FontStyle="Normal"
                                                        FontWeight="Normal"
                                                        HorizontalAlignment="Center"
                                                        VerticalAlignment="Center"/>
                                            </Setter.Value>
                                        </Setter>
                                    </Trigger>
                                    <Trigger Property="FlowDirection" Value="RightToLeft">
                                        <Setter Property="Content">
                                            <Setter.Value>
                                                <TextBlock
                                                        Text="{StaticResource GlyphArrow60Right}"
                                                        FontFamily="{StaticResource TelerikWebUI}"
                                                        Foreground="{Binding (TextElement.Foreground), RelativeSource={RelativeSource AncestorType=ContentPresenter}}"
                                                        FontSize="16"
                                                        FontStyle="Normal"
                                                        FontWeight="Normal"
                                                        HorizontalAlignment="Center"
                                                        VerticalAlignment="Center"/>
                                            </Setter.Value>
                                        </Setter>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </telerik:RadButton.Style>
                    </telerik:RadButton>

                    <!-- NumericPresenter -->
                    <Border VerticalAlignment="Center" Visibility="{Binding PagerControlsVisibility.NumericElementsControlVisibility,RelativeSource={RelativeSource TemplatedParent}}">
                        <dataPager:NumericElementsPresenter
                                NumericButtonStyle="{TemplateBinding NumericButtonStyle}"
                                AutoEllipsisMode="{TemplateBinding AutoEllipsisMode}"
                                PageIndex="{TemplateBinding PageIndex}"
                                PageCount="{TemplateBinding PageCount}"
                                NumericButtonCount="{TemplateBinding NumericButtonCount}"/>
                    </Border>

                    <!-- NextPage Button -->
                    <telerik:RadButton Name="MoveToNextPageButton" Command="{x:Static controls:RadDataPagerCommands.MoveToNextPage}" Visibility="{Binding PagerControlsVisibility.MoveToNextPageControlVisibility, RelativeSource={RelativeSource TemplatedParent}}">
                        <telerik:RadButton.Style>
                            <Style TargetType="{x:Type telerik:RadButton}" BasedOn="{StaticResource PageButtonStyle}">
                                <Style.Triggers>
                                    <Trigger Property="FlowDirection" Value="LeftToRight">
                                        <Setter Property="Content">
                                            <Setter.Value>
                                                <TextBlock
                                                        Text="{StaticResource GlyphArrow60Right}"
                                                        FontFamily="{StaticResource TelerikWebUI}"
                                                        Foreground="{Binding (TextElement.Foreground), RelativeSource={RelativeSource AncestorType=ContentPresenter}}"
                                                        FontSize="16"
                                                        FontStyle="Normal"
                                                        FontWeight="Normal"
                                                        HorizontalAlignment="Center"
                                                        VerticalAlignment="Center"/>
                                            </Setter.Value>
                                        </Setter>
                                    </Trigger>
                                    <Trigger Property="FlowDirection" Value="RightToLeft">
                                        <Setter Property="Content">
                                            <Setter.Value>
                                                <TextBlock
                                                        Text="{StaticResource GlyphArrow60Left}"
                                                        FontFamily="{StaticResource TelerikWebUI}"
                                                        Foreground="{Binding (TextElement.Foreground), RelativeSource={RelativeSource AncestorType=ContentPresenter}}"
                                                        FontSize="16"
                                                        FontStyle="Normal"
                                                        FontWeight="Normal"
                                                        HorizontalAlignment="Center"
                                                        VerticalAlignment="Center"/>
                                            </Setter.Value>
                                        </Setter>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </telerik:RadButton.Style>
                    </telerik:RadButton>

                    <!-- LastPage Button -->
                    <telerik:RadButton Name="MoveToLastPageButton" Command="{x:Static controls:RadDataPagerCommands.MoveToLastPage}" Visibility="{Binding PagerControlsVisibility.MoveToLastPageControlVisibility,RelativeSource={RelativeSource TemplatedParent}}">
                        <telerik:RadButton.Style>
                            <Style TargetType="{x:Type telerik:RadButton}" BasedOn="{StaticResource PageButtonStyle}">
                                <Style.Triggers>
                                    <Trigger Property="FlowDirection" Value="LeftToRight">
                                        <Setter Property="Content">
                                            <Setter.Value>
                                                <TextBlock
                                                        Text="{StaticResource GlyphArrowForward}"
                                                        FontFamily="{StaticResource TelerikWebUI}"
                                                        Foreground="{Binding (TextElement.Foreground), RelativeSource={RelativeSource AncestorType=ContentPresenter}}"
                                                        FontSize="16"
                                                        FontStyle="Normal"
                                                        FontWeight="Normal"
                                                        HorizontalAlignment="Center"
                                                        VerticalAlignment="Center"/>
                                            </Setter.Value>
                                        </Setter>
                                    </Trigger>
                                    <Trigger Property="FlowDirection" Value="RightToLeft">
                                        <Setter Property="Content">
                                            <Setter.Value>
                                                <TextBlock
                                                        Text="{StaticResource GlyphArrowBackward}"
                                                        FontFamily="{StaticResource TelerikWebUI}"
                                                        Foreground="{Binding (TextElement.Foreground), RelativeSource={RelativeSource AncestorType=ContentPresenter}}"
                                                        FontSize="16"
                                                        FontStyle="Normal"
                                                        FontWeight="Normal"
                                                        HorizontalAlignment="Center"
                                                        VerticalAlignment="Center"/>
                                            </Setter.Value>
                                        </Setter>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </telerik:RadButton.Style>
                    </telerik:RadButton>
                </StackPanel>
                <Grid
                        Background="Transparent"
                        Grid.Column="1"
                        Visibility="{Binding PagerControlsVisibility.TextControlVisibility,RelativeSource={RelativeSource TemplatedParent}}"
                        Margin="4 0 0 0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="auto"/>
                        <ColumnDefinition Width="auto"/>
                        <ColumnDefinition Width="auto"/>
                        <ColumnDefinition Width="auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock telerik:LocalizationManager.ResourceKey="RadDataPagerPage" TextAlignment="Center" VerticalAlignment="Center" Margin="4 0"/>
                    <dataPager:DataPagerTextBox
                            Grid.Column="1"
                            VerticalAlignment="Center"
                            Command="{x:Static controls:RadDataPagerCommands.MoveToPage}"
                            PageIndex="{TemplateBinding PageIndex}"
                            MinHeight="32"
                            MinWidth="32"/>
                    <TextBlock Grid.Column="2" telerik:LocalizationManager.ResourceKey="RadDataPagerOf" TextAlignment="Center" VerticalAlignment="Center" Margin="4 0"/>
                    <TextBlock Grid.Column="3" Text="{Binding PageCount, RelativeSource={RelativeSource TemplatedParent}}" TextAlignment="Center" VerticalAlignment="Center" Margin="2 0"/>
                </Grid>
            </Grid>
        </Border>
    </ControlTemplate>
    <Style x:Key="DataPagerPresenterStyle" TargetType="dataPager:DataPagerPresenter">
        <Setter Property="Template" Value="{StaticResource DataPagerPresenterTemplate}"/>
        <Setter Property="FontFamily" Value="{telerik1:Office2016TouchResource ResourceKey=FontFamily}"/>
        <Setter Property="FontSize" Value="{telerik1:Office2016TouchResource ResourceKey=FontSize}"/>
        <Setter Property="Foreground" Value="{telerik1:Office2016TouchResource ResourceKey=MarkerBrush}"/>
        <Setter Property="Padding" Value="4 0"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
    </Style>
    <ControlTemplate x:Key="RadDataPagerTemplate" TargetType="controls:RadDataPager">
        <dataPager:DataPagerPresenter
                NumericButtonStyle="{TemplateBinding NumericButtonStyle}"
                AutoEllipsisMode="{TemplateBinding AutoEllipsisMode}"
                DisplayMode="{TemplateBinding DisplayMode}"
                PageIndex="{TemplateBinding PageIndex}"
                PageCount="{TemplateBinding PageCount}"
                NumericButtonCount="{TemplateBinding NumericButtonCount}"
                Background="{TemplateBinding Background}"
                BorderBrush="{TemplateBinding BorderBrush}"
                BorderThickness="{TemplateBinding BorderThickness}"
                PageSize="{Binding PageSize, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"
                ItemCount="{Binding ItemCount, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"
                VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"
                HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                SnapsToDevicePixels="True"
                FocusVisualStyle="{x:Null}"/>
    </ControlTemplate>
    <Style x:Key="RadDataPagerStyle" TargetType="controls:RadDataPager">
        <Setter Property="FontFamily" Value="{telerik1:Office2016TouchResource ResourceKey=FontFamily}"/>
        <Setter Property="FontSize" Value="{telerik1:Office2016TouchResource ResourceKey=FontSize}"/>
        <Setter Property="Foreground" Value="{telerik1:Office2016TouchResource ResourceKey=MarkerBrush}"/>
        <Setter Property="Background" Value="{telerik1:Office2016TouchResource ResourceKey=MainBrush}"/>
        <Setter Property="BorderBrush" Value="{telerik1:Office2016TouchResource ResourceKey=BasicBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="MinHeight" Value="40"/>
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="NumericButtonStyle" Value="{StaticResource RadPageStyle}"/>
        <Setter Property="Template" Value="{StaticResource RadDataPagerTemplate}"/>
    </Style>
    <Style TargetType="controls:RadDataPager" BasedOn="{StaticResource RadDataPagerStyle}"/>
    <Style TargetType="dataPager:DataPagerPresenter" BasedOn="{StaticResource DataPagerPresenterStyle}"/>
    <Style TargetType="dataPager:NumericElementsPresenter" BasedOn="{StaticResource NumericElementsPresenterStyle}"/>
    <DataTemplate x:Key="FilterOperatorTemplate">
        <TextBlock Text="{Binding Converter={StaticResource FilterOperatorConverter}}"/>
    </DataTemplate>
    <ControlTemplate TargetType="telerik:RadToggleButton" x:Key="ToggleFilterButtonTemplate">
        <Grid>
            <ContentControl x:Name="Content"
                    Margin="{TemplateBinding Padding}"
                    Content="{TemplateBinding Content}"
                    Foreground="{TemplateBinding Foreground}"
                    ContentTemplate="{TemplateBinding ContentTemplate}"
                    VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                    HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"/>
        </Grid>
    </ControlTemplate>
    <Style x:Key="ToggleFilterButtonStyle" TargetType="telerik:RadToggleButton">
        <Setter Property="Template" Value="{StaticResource ToggleFilterButtonTemplate}"/>
        <Setter Property="FontFamily" Value="{telerik1:Office2016TouchResource ResourceKey=FontFamily}"/>
        <Setter Property="FontSize" Value="{telerik1:Office2016TouchResource ResourceKey=FontSize}"/>
        <Setter Property="Foreground" Value="{telerik1:Office2016TouchResource ResourceKey=MarkerBrush}"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="8 3"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
    </Style>
    <ControlTemplate x:Key="RemoveFilterButtonTemplate" TargetType="telerik:RadButton">
        <Border BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}">
            <TextBlock x:Name="GlyphVisual"
                    Text="{StaticResource GlyphClose}"
                    FontFamily="{StaticResource TelerikWebUI}"
                    Foreground="{TemplateBinding Foreground}"
                    FontStyle="Normal"
                    FontWeight="Normal"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    FontSize="32"/>
        </Border>
        <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="GlyphVisual" Property="Foreground" Value="{telerik1:Office2016TouchResource ResourceKey=ValidationBrush}"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter TargetName="GlyphVisual" Property="Foreground" Value="{telerik1:Office2016TouchResource ResourceKey=ValidationBrush}"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>
    <Style x:Key="RemoveFilterButtonStyle" TargetType="telerik:RadButton">
        <Setter Property="Template" Value="{StaticResource RemoveFilterButtonTemplate}"/>
        <Setter Property="Foreground" Value="{telerik1:Office2016TouchResource ResourceKey=IconBrush}"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Width" Value="32"/>
        <Setter Property="Height" Value="32"/>
    </Style>
    <ControlTemplate x:Key="AddFilterButtonTemplate" TargetType="telerik:RadButton">
        <Border BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}">
            <TextBlock x:Name="GlyphVisual"
                    Text="{StaticResource GlyphPlus}"
                    FontFamily="{StaticResource TelerikWebUI}"
                    Foreground="{TemplateBinding Foreground}"
                    FontStyle="Normal"
                    FontWeight="Normal"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    FontSize="32"/>
        </Border>
        <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="GlyphVisual" Property="Foreground" Value="{telerik1:Office2016TouchResource ResourceKey=AccentMouseOverBrush}"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter TargetName="GlyphVisual" Property="Foreground" Value="{telerik1:Office2016TouchResource ResourceKey=AccentMouseOverBrush}"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>
    <Style x:Key="AddFilterButtonStyle" TargetType="telerik:RadButton">
        <Setter Property="Template" Value="{StaticResource AddFilterButtonTemplate}"/>
        <Setter Property="Foreground" Value="{telerik1:Office2016TouchResource ResourceKey=IconBrush}"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Width" Value="32"/>
        <Setter Property="Height" Value="32"/>
    </Style>
    <ControlTemplate x:Key="FilterControlTemplate" TargetType="dataFilter:FilterControl">
        <Border x:Name="OuterBorder" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
            <StackPanel Orientation="Horizontal">
                <telerik:RadButton x:Name="PART_RemoveFilterButton" Command="{Binding RemoveFilter}" Style="{StaticResource RemoveFilterButtonStyle}"/>
                <StackPanel Orientation="Horizontal" Visibility="{Binding IsComposite, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <telerik:RadToggleButton
                            Name="PART_CompositeFilterLogicalOperatorToggleButton"
                            Style="{StaticResource ToggleFilterButtonStyle}"
                            IsChecked="{Binding CompositeFilter.LogicalOperator, Mode=TwoWay, Converter={StaticResource LogicalOperatorToCheckedConverter}}"
                            Content="{Binding CompositeFilter.LogicalOperator, Converter={StaticResource LogicalOperatorConverter}}"/>
                    <telerik:RadButton x:Name="PART_AddFilterButton" Command="{Binding AddFilter}" Style="{StaticResource AddFilterButtonStyle}"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Visibility="{Binding IsComposite, Converter={StaticResource InvertedBooleanToVisibilityConverter}}">
                    <telerik:RadComboBox
                            Name="PART_SimpleFilterMemberComboBox"
                            VerticalAlignment="Stretch"
                            Margin="0 0 4 0"
                            MinWidth="120"
                            ItemsSource="{Binding SimpleFilter.AvailableMembers}"
                            SelectedItem="{Binding SimpleFilter.SelectedMember, Mode=TwoWay}"/>
                    <telerik:RadComboBox
                            Name="PART_SimpleFilterOperatorComboBox"
                            VerticalAlignment="Stretch"
                            Margin="0 0 4 0"
                            MinWidth="120"
                            ItemsSource="{Binding SimpleFilter.AvailableOperators}"
                            SelectedItem="{Binding SimpleFilter.Operator, Mode=TwoWay, Converter={StaticResource FilterOperatorToSelectedItemConverter}}"
                            IsEnabled="{Binding SimpleFilter.Member, Converter={StaticResource FilterMemberToEnabledConverter}}"
                            ItemTemplate="{StaticResource FilterOperatorTemplate}"/>
                    <dataFilter:FilterEditor x:Name="filterEditor"
                            DataContext="{Binding SimpleFilter}"
                            ItemPropertyDefinition="{Binding SelectedMember}"
                            FilterOperator="{Binding Operator}"
                            HorizontalContentAlignment="Stretch"
                            IsTabStop="False"
                            VerticalContentAlignment="Stretch"
                            MinHeight="32"/>
                    <Border Width="8" BorderBrush="Transparent" Visibility="{Binding CanUserCreateCompositeFilters, Converter={StaticResource InvertedBooleanToVisibilityConverter}}"/>
                    <telerik:RadButton x:Name="PART_ToCompositeFilterButton"
                            Command="{Binding ToCompositeFilter}"
                            Style="{StaticResource AddFilterButtonStyle}"
                            Visibility="{Binding CanUserCreateCompositeFilters, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </ControlTemplate>
    <Style x:Key="FilterControlStyle" TargetType="dataFilter:FilterControl">
        <Setter Property="Template" Value="{StaticResource FilterControlTemplate}"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="IsTabStop" Value="False"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="VerticalAlignment" Value="Top"/>
        <Setter Property="HorizontalAlignment" Value="Left"/>
    </Style>
    <ControlTemplate x:Key="DataFilterPresenterTemplate" TargetType="dataFilter:DataFilterPresenter">
        <Border Padding="20 2 0 2">
            <Grid x:Name="Root">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <ContentControl x:Name="HeaderContent"
                        Grid.Row="0"
                        Content="{TemplateBinding Header}"
                        VerticalAlignment="Stretch"
                        ContentTemplate="{TemplateBinding HeaderTemplate}"
                        IsTabStop="False"/>
                <ItemsPresenter x:Name="Items" Grid.Row="1"/>
            </Grid>
        </Border>
    </ControlTemplate>
    <HierarchicalDataTemplate x:Key="DataFilterPresenterItemTemplate" ItemsSource="{Binding CompositeFilter.Filters}">
        <dataFilter:FilterControl/>
    </HierarchicalDataTemplate>
    <DataTemplate x:Key="DataFilterPresenterHeaderTemplate">
        <dataFilter:FilterControl/>
    </DataTemplate>
    <Style x:Key="DataFilterPresenterStyle" TargetType="dataFilter:DataFilterPresenter">
        <Setter Property="Template" Value="{StaticResource DataFilterPresenterTemplate}"/>
        <Setter Property="ItemTemplate" Value="{StaticResource DataFilterPresenterItemTemplate}"/>
        <Setter Property="HeaderTemplate" Value="{StaticResource DataFilterPresenterHeaderTemplate}"/>
        <Setter Property="IsTabStop" Value="False"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
    </Style>
    <ControlTemplate x:Key="RadDataFilterTemplate" TargetType="controls:RadDataFilter">
        <ScrollViewer ScrollViewer.HorizontalScrollBarVisibility="Auto" ScrollViewer.VerticalScrollBarVisibility="Auto" BorderThickness="0">
            <dataFilter:DataFilterPresenter DataContext="{TemplateBinding ViewModel}" Header="{Binding .}" ItemsSource="{Binding CompositeFilter.Filters}"/>
        </ScrollViewer>
    </ControlTemplate>
    <Style x:Key="RadDataFilterStyle" TargetType="controls:RadDataFilter">
        <Setter Property="Template" Value="{StaticResource RadDataFilterTemplate}"/>
        <Setter Property="IsTabStop" Value="False"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
    </Style>
    <Style TargetType="controls:RadDataFilter" BasedOn="{StaticResource RadDataFilterStyle}"/>
    <Style TargetType="dataFilter:FilterControl" BasedOn="{StaticResource FilterControlStyle}"/>
    <Style TargetType="dataFilter:DataFilterPresenter" BasedOn="{StaticResource DataFilterPresenterStyle}"/>
    <ControlTemplate x:Key="DataFormValidationSummaryTemplate" TargetType="dataForm:DataFormValidationSummary">
        <ItemsControl IsTabStop="False" ItemsSource="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=Errors}">
            <ItemsControl.Style>
                <Style TargetType="ItemsControl">
                    <Setter Property="BorderBrush" Value="{telerik1:Office2016TouchResource ResourceKey=BasicBrush}"/>
                    <Setter Property="BorderThickness" Value="0 1 0 0"/>
                    <Setter Property="Padding" Value="4 4 4 5"/>
                    <Setter Property="MaxHeight" Value="90"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate>
                                <ScrollViewer x:Name="ScrollViewer"
                                        IsTabStop="False"
                                        ScrollViewer.VerticalScrollBarVisibility="Auto"
                                        ScrollViewer.HorizontalScrollBarVisibility="Auto"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}">
                                    <ItemsPresenter x:Name="Content" Margin="{TemplateBinding Padding}"/>
                                </ScrollViewer>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Setter Property="ItemTemplate">
                        <Setter.Value>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                    <TextBlock Foreground="{telerik1:Office2016TouchResource ResourceKey=ValidationBrush}" Text="{Binding SourceFieldDisplayName}"/>
                                    <TextBlock Text=":" Margin="4 0" Foreground="{telerik1:Office2016TouchResource ResourceKey=ValidationBrush}"/>
                                    <TextBlock Text="{Binding ErrorContent}"/>
                                </StackPanel>
                            </DataTemplate>
                        </Setter.Value>
                    </Setter>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding HasItems, RelativeSource={RelativeSource Self}}" Value="False">
                            <Setter Property="Padding" Value="0"/>
                            <Setter Property="BorderThickness" Value="0"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </ItemsControl.Style>
        </ItemsControl>
    </ControlTemplate>
    <Style x:Key="DataFormValidationSummaryStyle" TargetType="dataForm:DataFormValidationSummary">
        <Setter Property="Template" Value="{StaticResource DataFormValidationSummaryTemplate}"/>
        <Setter Property="IsTabStop" Value="False"/>
    </Style>
    <ControlTemplate x:Key="CollectionNavigatorButtonTemplate" TargetType="telerik:RadButton">
        <Grid>
            <Border x:Name="BorderVisual"
                    BorderThickness="{TemplateBinding BorderThickness}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    Background="{TemplateBinding Background}"
                    CornerRadius="{TemplateBinding CornerRadius}"
                    SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}">
                <ContentPresenter x:Name="Content"
                        TextBlock.Foreground="{TemplateBinding Foreground}"
                        Margin="{TemplateBinding Padding}"
                        Content="{TemplateBinding Content}"
                        ContentTemplate="{TemplateBinding ContentTemplate}"
                        VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                        HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                        ContentTemplateSelector="{TemplateBinding ContentTemplateSelector}"
                        ContentStringFormat="{TemplateBinding ContentStringFormat}"
                        RecognizesAccessKey="True"/>
            </Border>
            <Border x:Name="FocusVisual"
                    Opacity="0"
                    BorderBrush="{telerik1:Office2016TouchResource ResourceKey=AccentFocusedBrush}"
                    BorderThickness="{telerik1:Office2016TouchResource ResourceKey=FocusThickness}"
                    CornerRadius="{TemplateBinding CornerRadius}"
                    SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
        </Grid>
        <ControlTemplate.Triggers>
            <Trigger Property="IsFocused" Value="True">
                <Setter TargetName="FocusVisual" Property="Opacity" Value="1"/>
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="Content" Property="TextElement.Foreground" Value="{telerik1:Office2016TouchResource ResourceKey=MarkerInvertedBrush}"/>
                <Setter TargetName="BorderVisual" Property="Background" Value="{telerik1:Office2016TouchResource ResourceKey=AccentMouseOverBrush}"/>
                <Setter TargetName="BorderVisual" Property="BorderBrush" Value="{telerik1:Office2016TouchResource ResourceKey=AccentMouseOverBrush}"/>
                <Setter TargetName="FocusVisual" Property="Opacity" Value="0"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter TargetName="Content" Property="TextElement.Foreground" Value="{telerik1:Office2016TouchResource ResourceKey=MarkerInvertedBrush}"/>
                <Setter TargetName="BorderVisual" Property="Background" Value="{telerik1:Office2016TouchResource ResourceKey=AccentPressedBrush}"/>
                <Setter TargetName="BorderVisual" Property="BorderBrush" Value="{telerik1:Office2016TouchResource ResourceKey=AccentPressedBrush}"/>
                <Setter TargetName="FocusVisual" Property="Opacity" Value="0"/>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter TargetName="Content" Property="Opacity" Value="{telerik1:Office2016TouchResource ResourceKey=DisabledOpacity}"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>
    <Style TargetType="telerik:RadButton" x:Key="CollectionNavigatorButtonStyle">
        <Setter Property="Template" Value="{StaticResource CollectionNavigatorButtonTemplate}"/>
        <Setter Property="Foreground" Value="{telerik1:Office2016TouchResource ResourceKey=IconBrush}"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="Margin" Value="0 0 0 4"/>
        <Setter Property="Width" Value="32"/>
        <Setter Property="Height" Value="32"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="CornerRadius" Value="{telerik1:Office2016TouchResource ResourceKey=CornerRadius}"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
    </Style>
    <ControlTemplate x:Key="CollectionNavigatorTemplate" TargetType="dataForm:CollectionNavigator">
        <Border x:Name="PART_RootElement"
                BorderThickness="{TemplateBinding BorderThickness}"
                Background="{TemplateBinding Background}"
                BorderBrush="{TemplateBinding BorderBrush}"
                UseLayoutRounding="True"
                CornerRadius="{telerik1:Office2016TouchResource ResourceKey=CornerRadius}">
            <Grid x:Name="PART_CollectionNavigatorGrid" Margin="0 0 0 6">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="4 0 0 0">
                    <telerik:RadButton x:Name="MoveCurrentToFirst"
                            Command="{x:Static controls:RadDataFormCommands.MoveCurrentToFirst}"
                            Visibility="{Binding NavigationButtonsVisibility}"
                            RenderTransformOrigin="0.5,0.5">
                        <ToolTipService.ToolTip>
                            <ToolTip telerik:LocalizationManager.ResourceKey="DataForm_MoveCurrentToFirst"/>
                        </ToolTipService.ToolTip>
                        <telerik:RadButton.Style>
                            <Style TargetType="{x:Type telerik:RadButton}" BasedOn="{StaticResource CollectionNavigatorButtonStyle}">
                                <Style.Triggers>
                                    <Trigger Property="FlowDirection" Value="LeftToRight">
                                        <Setter Property="Content">
                                            <Setter.Value>
                                                <TextBlock
                                                        Text="{StaticResource GlyphArrowBackward}"
                                                        FontFamily="{StaticResource TelerikWebUI}"
                                                        Foreground="{Binding (TextElement.Foreground), RelativeSource={RelativeSource AncestorType=ContentPresenter}}"
                                                        FontSize="16"
                                                        FontStyle="Normal"
                                                        FontWeight="Normal"
                                                        HorizontalAlignment="Center"
                                                        VerticalAlignment="Center"/>
                                            </Setter.Value>
                                        </Setter>
                                    </Trigger>
                                    <Trigger Property="FlowDirection" Value="RightToLeft">
                                        <Setter Property="Content">
                                            <Setter.Value>
                                                <TextBlock
                                                        Text="{StaticResource GlyphArrowForward}"
                                                        FontFamily="{StaticResource TelerikWebUI}"
                                                        Foreground="{Binding (TextElement.Foreground), RelativeSource={RelativeSource AncestorType=ContentPresenter}}"
                                                        FontSize="16"
                                                        FontStyle="Normal"
                                                        FontWeight="Normal"
                                                        HorizontalAlignment="Center"
                                                        VerticalAlignment="Center"/>
                                            </Setter.Value>
                                        </Setter>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </telerik:RadButton.Style>
                    </telerik:RadButton>
                    <telerik:RadButton x:Name="MoveCurrentToPrevious" Command="{x:Static controls:RadDataFormCommands.MoveCurrentToPrevious}" Visibility="{Binding NavigationButtonsVisibility}">
                        <ToolTipService.ToolTip>
                            <ToolTip telerik:LocalizationManager.ResourceKey="DataForm_MoveCurrentToPrevious"/>
                        </ToolTipService.ToolTip>
                        <telerik:RadButton.Style>
                            <Style TargetType="{x:Type telerik:RadButton}" BasedOn="{StaticResource CollectionNavigatorButtonStyle}">
                                <Style.Triggers>
                                    <Trigger Property="FlowDirection" Value="LeftToRight">
                                        <Setter Property="Content">
                                            <Setter.Value>
                                                <TextBlock
                                                        Text="{StaticResource GlyphArrow60Left}"
                                                        FontFamily="{StaticResource TelerikWebUI}"
                                                        Foreground="{Binding (TextElement.Foreground), RelativeSource={RelativeSource AncestorType=ContentPresenter}}"
                                                        FontSize="16"
                                                        FontStyle="Normal"
                                                        FontWeight="Normal"
                                                        HorizontalAlignment="Center"
                                                        VerticalAlignment="Center"/>
                                            </Setter.Value>
                                        </Setter>
                                    </Trigger>
                                    <Trigger Property="FlowDirection" Value="RightToLeft">
                                        <Setter Property="Content">
                                            <Setter.Value>
                                                <TextBlock
                                                        Text="{StaticResource GlyphArrow60Right}"
                                                        FontFamily="{StaticResource TelerikWebUI}"
                                                        Foreground="{Binding (TextElement.Foreground), RelativeSource={RelativeSource AncestorType=ContentPresenter}}"
                                                        FontSize="16"
                                                        FontStyle="Normal"
                                                        FontWeight="Normal"
                                                        HorizontalAlignment="Center"
                                                        VerticalAlignment="Center"/>
                                            </Setter.Value>
                                        </Setter>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </telerik:RadButton.Style>
                    </telerik:RadButton>
                    <telerik:RadButton x:Name="MoveCurrentToNext" Command="{x:Static controls:RadDataFormCommands.MoveCurrentToNext}" Visibility="{Binding NavigationButtonsVisibility}">
                        <ToolTipService.ToolTip>
                            <ToolTip telerik:LocalizationManager.ResourceKey="DataForm_MoveCurrentToNext"/>
                        </ToolTipService.ToolTip>
                        <telerik:RadButton.Style>
                            <Style TargetType="{x:Type telerik:RadButton}" BasedOn="{StaticResource CollectionNavigatorButtonStyle}">
                                <Style.Triggers>
                                    <Trigger Property="FlowDirection" Value="LeftToRight">
                                        <Setter Property="Content">
                                            <Setter.Value>
                                                <TextBlock
                                                        Text="{StaticResource GlyphArrow60Right}"
                                                        FontFamily="{StaticResource TelerikWebUI}"
                                                        Foreground="{Binding (TextElement.Foreground), RelativeSource={RelativeSource AncestorType=ContentPresenter}}"
                                                        FontSize="16"
                                                        FontStyle="Normal"
                                                        FontWeight="Normal"
                                                        HorizontalAlignment="Center"
                                                        VerticalAlignment="Center"/>
                                            </Setter.Value>
                                        </Setter>
                                    </Trigger>
                                    <Trigger Property="FlowDirection" Value="RightToLeft">
                                        <Setter Property="Content">
                                            <Setter.Value>
                                                <TextBlock
                                                        Text="{StaticResource GlyphArrow60Left}"
                                                        FontFamily="{StaticResource TelerikWebUI}"
                                                        Foreground="{Binding (TextElement.Foreground), RelativeSource={RelativeSource AncestorType=ContentPresenter}}"
                                                        FontSize="16"
                                                        FontStyle="Normal"
                                                        FontWeight="Normal"
                                                        HorizontalAlignment="Center"
                                                        VerticalAlignment="Center"/>
                                            </Setter.Value>
                                        </Setter>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </telerik:RadButton.Style>
                    </telerik:RadButton>
                    <telerik:RadButton x:Name="MoveCurrentToLast" Command="{x:Static controls:RadDataFormCommands.MoveCurrentToLast}" Visibility="{Binding NavigationButtonsVisibility}">
                        <ToolTipService.ToolTip>
                            <ToolTip telerik:LocalizationManager.ResourceKey="DataForm_MoveCurrentToLast"/>
                        </ToolTipService.ToolTip>
                        <telerik:RadButton.Style>
                            <Style TargetType="{x:Type telerik:RadButton}" BasedOn="{StaticResource CollectionNavigatorButtonStyle}">
                                <Style.Triggers>
                                    <Trigger Property="FlowDirection" Value="LeftToRight">
                                        <Setter Property="Content">
                                            <Setter.Value>
                                                <TextBlock
                                                        Text="{StaticResource GlyphArrowForward}"
                                                        FontFamily="{StaticResource TelerikWebUI}"
                                                        Foreground="{Binding (TextElement.Foreground), RelativeSource={RelativeSource AncestorType=ContentPresenter}}"
                                                        FontSize="16"
                                                        FontStyle="Normal"
                                                        FontWeight="Normal"
                                                        HorizontalAlignment="Center"
                                                        VerticalAlignment="Center"/>
                                            </Setter.Value>
                                        </Setter>
                                    </Trigger>
                                    <Trigger Property="FlowDirection" Value="RightToLeft">
                                        <Setter Property="Content">
                                            <Setter.Value>
                                                <TextBlock
                                                        Text="{StaticResource GlyphArrowBackward}"
                                                        FontFamily="{StaticResource TelerikWebUI}"
                                                        Foreground="{Binding (TextElement.Foreground), RelativeSource={RelativeSource AncestorType=ContentPresenter}}"
                                                        FontSize="16"
                                                        FontStyle="Normal"
                                                        FontWeight="Normal"
                                                        HorizontalAlignment="Center"
                                                        VerticalAlignment="Center"/>
                                            </Setter.Value>
                                        </Setter>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </telerik:RadButton.Style>
                    </telerik:RadButton>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Grid.Column="1" HorizontalAlignment="Right" Margin="0 0 4 0">
                    <telerik:RadButton x:Name="AddNew"
                            Style="{StaticResource CollectionNavigatorButtonStyle}"
                            Visibility="{Binding AddNewButtonVisibility}"
                            Command="{x:Static controls:RadDataFormCommands.AddNew}">
                        <ToolTipService.ToolTip>
                            <ToolTip telerik:LocalizationManager.ResourceKey="DataForm_AddNew"/>
                        </ToolTipService.ToolTip>
                        <TextBlock
                                Text="{StaticResource GlyphFile}"
                                FontFamily="{StaticResource TelerikWebUI}"
                                Foreground="{Binding (TextElement.Foreground), RelativeSource={RelativeSource AncestorType=ContentPresenter}}"
                                FontSize="16"
                                FontStyle="Normal"
                                FontWeight="Normal"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"/>
                    </telerik:RadButton>
                    <telerik:RadButton x:Name="BeginEdit"
                            Command="{x:Static controls:RadDataFormCommands.BeginEdit}"
                            Style="{StaticResource CollectionNavigatorButtonStyle}"
                            Visibility="{Binding EditButtonVisibility}"
                            RenderTransformOrigin="0.5,0.5">
                        <ToolTipService.ToolTip>
                            <ToolTip telerik:LocalizationManager.ResourceKey="DataForm_BeginEdit"/>
                        </ToolTipService.ToolTip>
                        <TextBlock
                                Text="{StaticResource GlyphEdit}"
                                FontFamily="{StaticResource TelerikWebUI}"
                                Foreground="{Binding (TextElement.Foreground), RelativeSource={RelativeSource AncestorType=ContentPresenter}}"
                                FontSize="16"
                                FontStyle="Normal"
                                FontWeight="Normal"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"/>
                    </telerik:RadButton>
                    <telerik:RadButton x:Name="Delete"
                            Command="{x:Static controls:RadDataFormCommands.Delete}"
                            Style="{StaticResource CollectionNavigatorButtonStyle}"
                            Visibility="{Binding DeleteButtonVisibility}"
                            RenderTransformOrigin="0.5,0.5">
                        <ToolTipService.ToolTip>
                            <ToolTip telerik:LocalizationManager.ResourceKey="DataForm_Delete"/>
                        </ToolTipService.ToolTip>
                        <TextBlock
                                Text="{StaticResource GlyphDelete}"
                                FontFamily="{StaticResource TelerikWebUI}"
                                Foreground="{Binding (TextElement.Foreground), RelativeSource={RelativeSource AncestorType=ContentPresenter}}"
                                FontSize="16"
                                FontStyle="Normal"
                                FontWeight="Normal"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"/>
                    </telerik:RadButton>
                </StackPanel>
            </Grid>
        </Border>
    </ControlTemplate>
    <Style x:Key="CollectionNavigatorStyle" TargetType="dataForm:CollectionNavigator">
        <Setter Property="Template" Value="{StaticResource CollectionNavigatorTemplate}"/>
        <Setter Property="Background" Value="{telerik1:Office2016TouchResource ResourceKey=MainBrush}"/>
        <Setter Property="IsTabStop" Value="False"/>
    </Style>
    <ControlTemplate x:Key="AutoGeneratedFieldsPresenterTemplate" TargetType="dataForm:AutoGeneratedFieldsPresenter">
        <ItemsControl IsTabStop="False" ItemsSource="{TemplateBinding Fields}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <VirtualizingStackPanel Orientation="Vertical"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
        </ItemsControl>
    </ControlTemplate>
    <Style x:Key="AutoGeneratedFieldsPresenterStyle" TargetType="dataForm:AutoGeneratedFieldsPresenter">
        <Setter Property="Template" Value="{StaticResource AutoGeneratedFieldsPresenterTemplate}"/>
        <Setter Property="IsTabStop" Value="False"/>
    </Style>
    <ControlTemplate x:Key="DataFormDataFieldTemplate" TargetType="controls:DataFormDataField">
        <Border x:Name="PART_RootElement"
                Background="{TemplateBinding Background}"
                BorderBrush="{TemplateBinding BorderBrush}"
                BorderThickness="{TemplateBinding BorderThickness}"
                UseLayoutRounding="True"
                CornerRadius="{telerik1:Office2016TouchResource ResourceKey=CornerRadius}">
            <Grid x:Name="PART_DataFormDataFieldGrid" Margin="{TemplateBinding Padding}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="32"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <ContentControl x:Name="PART_Label"
                        IsTabStop="False"
                        Margin="4 0"
                        VerticalAlignment="Center"
                        VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"
                        HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                        Content="{TemplateBinding Label}"
                        Foreground="{TemplateBinding Foreground}"
                        ContentTemplate="{TemplateBinding LabelTemplate}"/>
                <ContentPresenter
                        Grid.Row="1"
                        x:Name="PART_ContentPresenter"
                        Grid.Column="1"
                        Content="{TemplateBinding Content}"
                        VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                        HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"/>
                <Grid
                        Grid.Row="1"
                        x:Name="DescriptionIcon"
                        Grid.Column="2"
                        Visibility="{Binding Description, Converter={StaticResource DescriptionVisibilityConverter}, RelativeSource={RelativeSource TemplatedParent}}"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Background="Transparent"
                        Margin="3 0 0 2">
                    <ToolTipService.ToolTip>
                        <ToolTip Content="{TemplateBinding Description}"/>
                    </ToolTipService.ToolTip>
                    <TextBlock
                            Text="{StaticResource GlyphQuestion}"
                            FontFamily="{StaticResource TelerikWebUI}"
                            Foreground="{telerik1:Office2016TouchResource ResourceKey=IconBrush}"
                            FontStyle="Normal"
                            FontWeight="Normal"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Margin="1 0 0 0"
                            FontSize="24"/>
                </Grid>
            </Grid>
        </Border>
    </ControlTemplate>
    <Style x:Key="DataFormDataFieldStyle" TargetType="controls:DataFormDataField">
        <Setter Property="Template" Value="{StaticResource DataFormDataFieldTemplate}"/>
        <Setter Property="FontFamily" Value="{telerik1:Office2016TouchResource ResourceKey=FontFamily}"/>
        <Setter Property="FontSize" Value="{telerik1:Office2016TouchResource ResourceKey=FontSize}"/>
        <Setter Property="Foreground" Value="{telerik1:Office2016TouchResource ResourceKey=MarkerBrush}"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Padding" Value="4 0 4 4"/>
        <Setter Property="IsTabStop" Value="False"/>
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
    </Style>
    <ControlTemplate x:Key="RadDataFormTemplate" TargetType="controls:RadDataForm">
        <Border x:Name="PART_RootElement"
                BorderBrush="{TemplateBinding BorderBrush}"
                BorderThickness="{TemplateBinding BorderThickness}"
                UseLayoutRounding="True"
                CornerRadius="{telerik1:Office2016TouchResource ResourceKey=CornerRadius}">
            <Grid x:Name="PART_DataFormGrid">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Border x:Name="BorderVisual" Background="{TemplateBinding Background}" Grid.RowSpan="5" CornerRadius="{telerik1:Office2016TouchResource ResourceKey=CornerRadius}"/>
                <Border x:Name="Header"
                        Visibility="{Binding Header, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource DescriptionVisibilityConverter}}"
                        CornerRadius="{telerik1:Office2016TouchResource ResourceKey=CornerRadiusTop}"
                        MinHeight="36">
                    <ContentControl x:Name="PART_ContentPresenter"
                            Margin="4"
                            VerticalContentAlignment="Top"
                            HorizontalContentAlignment="Stretch"
                            Content="{TemplateBinding Header}"
                            VerticalAlignment="Center"/>
                </Border>
                <dataForm:CollectionNavigator x:Name="CollectionNavigator" Grid.Row="1"/>
                <ScrollViewer x:Name="PART_ItemsScrollViewer"
                        Grid.Row="2"
                        IsTabStop="False"
                        BorderBrush="Transparent"
                        ScrollViewer.HorizontalScrollBarVisibility="Auto"
                        ScrollViewer.VerticalScrollBarVisibility="Auto">
                    <StackPanel Orientation="Vertical">
                        <dataForm:AutoGeneratedFieldsPresenter x:Name="PART_AutoGeneratedFieldsPresenter" AutoGenerateFields="{Binding AutoGenerateFields,RelativeSource={RelativeSource TemplatedParent}}"/>
                        <ContentPresenter x:Name="PART_FieldsContentPresenter"/>
                    </StackPanel>
                </ScrollViewer>
                <dataForm:DataFormValidationSummary x:Name="PART_ValidationSummary" Grid.Row="3" Visibility="{TemplateBinding ValidationSummaryVisibility}"/>
                <Border x:Name="FooterPanel_Background"
                        Grid.Row="4"
                        Background="{telerik1:Office2016TouchResource ResourceKey=AlternativeBrush}"
                        BorderBrush="{telerik1:Office2016TouchResource ResourceKey=BasicBrush}"
                        BorderThickness="0 1 0 0">
                    <StackPanel x:Name="PART_FooterPanel" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0 7">
                        <telerik:RadButton x:Name="PART_CommitButton"
                                Command="{x:Static controls:RadDataFormCommands.CommitEdit}"
                                MinWidth="100"
                                Margin="0 0 7 0"
                                Content="{TemplateBinding CommitButtonContent}"
                                VerticalAlignment="Center"/>
                        <telerik:RadButton x:Name="PART_CancelButton"
                                Command="{x:Static controls:RadDataFormCommands.CancelEdit}"
                                MinWidth="100"
                                Margin="0 0 7 0"
                                Content="{TemplateBinding CancelButtonContent}"
                                VerticalAlignment="Center"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>
        <ControlTemplate.Triggers>
            <Trigger Property="IsEnabled" Value="False">
                <Setter TargetName="PART_DataFormGrid" Property="Opacity" Value="{telerik1:Office2016TouchResource ResourceKey=DisabledOpacity}"/>
                <Setter TargetName="PART_DataFormGrid" Property="Background" Value="{telerik1:Office2016TouchResource ResourceKey=PrimaryBrush}"/>
                <Setter TargetName="BorderVisual" Property="Background" Value="Transparent"/>
                <Setter TargetName="CollectionNavigator" Property="Background" Value="Transparent"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>
    <Style x:Key="RadDataFormStyle" TargetType="controls:RadDataForm">
        <Setter Property="Template" Value="{StaticResource RadDataFormTemplate}"/>
        <Setter Property="FontFamily" Value="{telerik1:Office2016TouchResource ResourceKey=FontFamily}"/>
        <Setter Property="FontSize" Value="{telerik1:Office2016TouchResource ResourceKey=FontSize}"/>
        <Setter Property="Foreground" Value="{telerik1:Office2016TouchResource ResourceKey=MarkerBrush}"/>
        <Setter Property="Background" Value="{telerik1:Office2016TouchResource ResourceKey=MainBrush}"/>
        <Setter Property="BorderBrush" Value="{telerik1:Office2016TouchResource ResourceKey=BasicBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
    </Style>
    <Style TargetType="controls:RadDataForm" BasedOn="{StaticResource RadDataFormStyle}"/>
    <Style TargetType="controls:DataFormDataField" BasedOn="{StaticResource DataFormDataFieldStyle}"/>
    <Style TargetType="dataForm:CollectionNavigator" BasedOn="{StaticResource CollectionNavigatorStyle}"/>
    <Style TargetType="dataForm:AutoGeneratedFieldsPresenter" BasedOn="{StaticResource AutoGeneratedFieldsPresenterStyle}"/>
    <Style TargetType="dataForm:DataFormValidationSummary" BasedOn="{StaticResource DataFormValidationSummaryStyle}"/>
    <Style TargetType="controls:DataFormCheckBoxField" BasedOn="{StaticResource DataFormDataFieldStyle}"/>
    <Style TargetType="controls:DataFormComboBoxField" BasedOn="{StaticResource DataFormDataFieldStyle}"/>
    <Style TargetType="controls:DataFormDateField" BasedOn="{StaticResource DataFormDataFieldStyle}"/>
    <ControlTemplate TargetType="telerik:RadButton" x:Key="RadCollectionNavigatorButtonTemplate">
        <Grid>
            <Border x:Name="BorderVisual"
                    BorderThickness="{TemplateBinding BorderThickness}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    Background="{TemplateBinding Background}"
                    CornerRadius="{TemplateBinding CornerRadius}"
                    SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}">
                <ContentPresenter x:Name="Content"
                        TextBlock.Foreground="{TemplateBinding Foreground}"
                        Margin="{TemplateBinding Padding}"
                        Content="{TemplateBinding Content}"
                        ContentTemplate="{TemplateBinding ContentTemplate}"
                        VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                        HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                        ContentTemplateSelector="{TemplateBinding ContentTemplateSelector}"
                        ContentStringFormat="{TemplateBinding ContentStringFormat}"
                        RecognizesAccessKey="True"/>
            </Border>
            <Border x:Name="FocusVisual"
                    Opacity="0"
                    BorderBrush="{telerik1:Office2016TouchResource ResourceKey=AccentFocusedBrush}"
                    BorderThickness="{telerik1:Office2016TouchResource ResourceKey=FocusThickness}"
                    CornerRadius="{TemplateBinding CornerRadius}"
                    SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
        </Grid>
        <ControlTemplate.Triggers>
            <Trigger Property="IsFocused" Value="True">
                <Setter TargetName="FocusVisual" Property="Opacity" Value="1"/>
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="BorderVisual" Property="Background" Value="{telerik1:Office2016TouchResource ResourceKey=MouseOverBrush}"/>
                <Setter TargetName="BorderVisual" Property="BorderBrush" Value="{telerik1:Office2016TouchResource ResourceKey=MouseOverBrush}"/>
                <Setter TargetName="FocusVisual" Property="Opacity" Value="0"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter TargetName="BorderVisual" Property="Background" Value="{telerik1:Office2016TouchResource ResourceKey=PressedBrush}"/>
                <Setter TargetName="BorderVisual" Property="BorderBrush" Value="{telerik1:Office2016TouchResource ResourceKey=PressedBrush}"/>
                <Setter TargetName="FocusVisual" Property="Opacity" Value="0"/>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter TargetName="Content" Property="Opacity" Value="{telerik1:Office2016TouchResource ResourceKey=DisabledOpacity}"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>
    <Style x:Key="RadCollectionNavigatorButtonStyle" TargetType="telerik:RadButton">
        <Setter Property="Template" Value="{StaticResource RadCollectionNavigatorButtonTemplate}"/>
        <Setter Property="Foreground" Value="{telerik1:Office2016TouchResource ResourceKey=IconBrush}"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="4 0"/>
        <Setter Property="Width" Value="32"/>
        <Setter Property="Height" Value="32"/>
        <Setter Property="Margin" Value="0"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="CornerRadius" Value="{telerik1:Office2016TouchResource ResourceKey=CornerRadius}"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
    </Style>
    <ControlTemplate x:Key="Collection_NavigatorTemplate" TargetType="controls:RadCollectionNavigator">
        <Border x:Name="PART_RootElement"
                BorderThickness="{TemplateBinding BorderThickness}"
                Background="{TemplateBinding Background}"
                BorderBrush="{TemplateBinding BorderBrush}"
                UseLayoutRounding="True">
            <Grid x:Name="PART_CollectionNavigatorGrid">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                    <telerik:RadButton x:Name="MoveCurrentToFirst" Visibility="{Binding Converter={StaticResource VisibilityConverter}, RelativeSource={RelativeSource TemplatedParent}, Path=CommandButtonsVisibility, ConverterParameter=first}" Command="{x:Static controls:RadCollectionNavigatorCommands.MoveCurrentToFirst}">
                        <ToolTipService.ToolTip>
                            <ToolTip telerik:LocalizationManager.ResourceKey="DataForm_MoveCurrentToFirst"/>
                        </ToolTipService.ToolTip>
                        <telerik:RadButton.Style>
                            <Style TargetType="{x:Type telerik:RadButton}" BasedOn="{StaticResource RadCollectionNavigatorButtonStyle}">
                                <Style.Triggers>
                                    <Trigger Property="FlowDirection" Value="LeftToRight">
                                        <Setter Property="Content">
                                            <Setter.Value>
                                                <TextBlock
                                                        Text="{StaticResource GlyphArrowBackward}"
                                                        FontFamily="{StaticResource TelerikWebUI}"
                                                        Foreground="{Binding (TextElement.Foreground), RelativeSource={RelativeSource AncestorType=ContentPresenter}}"
                                                        FontSize="16"
                                                        FontStyle="Normal"
                                                        FontWeight="Normal"
                                                        HorizontalAlignment="Center"
                                                        VerticalAlignment="Center"/>
                                            </Setter.Value>
                                        </Setter>
                                    </Trigger>
                                    <Trigger Property="FlowDirection" Value="RightToLeft">
                                        <Setter Property="Content">
                                            <Setter.Value>
                                                <TextBlock
                                                        Text="{StaticResource GlyphArrowForward}"
                                                        FontFamily="{StaticResource TelerikWebUI}"
                                                        Foreground="{Binding (TextElement.Foreground), RelativeSource={RelativeSource AncestorType=ContentPresenter}}"
                                                        FontSize="16"
                                                        FontStyle="Normal"
                                                        FontWeight="Normal"
                                                        HorizontalAlignment="Center"
                                                        VerticalAlignment="Center"/>
                                            </Setter.Value>
                                        </Setter>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </telerik:RadButton.Style>
                    </telerik:RadButton>
                    <telerik:RadButton x:Name="MoveCurrentToPrevious" Command="{x:Static controls:RadCollectionNavigatorCommands.MoveCurrentToPrevious}" Visibility="{Binding Converter={StaticResource VisibilityConverter}, RelativeSource={RelativeSource TemplatedParent}, Path=CommandButtonsVisibility, ConverterParameter=previous}">
                        <ToolTipService.ToolTip>
                            <ToolTip telerik:LocalizationManager.ResourceKey="DataForm_MoveCurrentToPrevious"/>
                        </ToolTipService.ToolTip>
                        <telerik:RadButton.Style>
                            <Style TargetType="{x:Type telerik:RadButton}" BasedOn="{StaticResource RadCollectionNavigatorButtonStyle}">
                                <Style.Triggers>
                                    <Trigger Property="FlowDirection" Value="LeftToRight">
                                        <Setter Property="Content">
                                            <Setter.Value>
                                                <TextBlock
                                                        Text="{StaticResource GlyphArrow60Left}"
                                                        FontFamily="{StaticResource TelerikWebUI}"
                                                        Foreground="{Binding (TextElement.Foreground), RelativeSource={RelativeSource AncestorType=ContentPresenter}}"
                                                        FontSize="16"
                                                        FontStyle="Normal"
                                                        FontWeight="Normal"
                                                        HorizontalAlignment="Center"
                                                        VerticalAlignment="Center"/>
                                            </Setter.Value>
                                        </Setter>
                                    </Trigger>
                                    <Trigger Property="FlowDirection" Value="RightToLeft">
                                        <Setter Property="Content">
                                            <Setter.Value>
                                                <TextBlock
                                                        Text="{StaticResource GlyphArrow60Right}"
                                                        FontFamily="{StaticResource TelerikWebUI}"
                                                        Foreground="{Binding (TextElement.Foreground), RelativeSource={RelativeSource AncestorType=ContentPresenter}}"
                                                        FontSize="16"
                                                        FontStyle="Normal"
                                                        FontWeight="Normal"
                                                        HorizontalAlignment="Center"
                                                        VerticalAlignment="Center"/>
                                            </Setter.Value>
                                        </Setter>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </telerik:RadButton.Style>
                    </telerik:RadButton>
                    <telerik:RadButton x:Name="MoveCurrentToNext" Visibility="{Binding Converter={StaticResource VisibilityConverter}, RelativeSource={RelativeSource TemplatedParent}, Path=CommandButtonsVisibility, ConverterParameter=next}" Command="{x:Static controls:RadCollectionNavigatorCommands.MoveCurrentToNext}">
                        <ToolTipService.ToolTip>
                            <ToolTip telerik:LocalizationManager.ResourceKey="DataForm_MoveCurrentToNext"/>
                        </ToolTipService.ToolTip>
                        <telerik:RadButton.Style>
                            <Style TargetType="{x:Type telerik:RadButton}" BasedOn="{StaticResource RadCollectionNavigatorButtonStyle}">
                                <Style.Triggers>
                                    <Trigger Property="FlowDirection" Value="LeftToRight">
                                        <Setter Property="Content">
                                            <Setter.Value>
                                                <TextBlock
                                                        Text="{StaticResource GlyphArrow60Right}"
                                                        FontFamily="{StaticResource TelerikWebUI}"
                                                        Foreground="{Binding (TextElement.Foreground), RelativeSource={RelativeSource AncestorType=ContentPresenter}}"
                                                        FontSize="16"
                                                        FontStyle="Normal"
                                                        FontWeight="Normal"
                                                        HorizontalAlignment="Center"
                                                        VerticalAlignment="Center"/>
                                            </Setter.Value>
                                        </Setter>
                                    </Trigger>
                                    <Trigger Property="FlowDirection" Value="RightToLeft">
                                        <Setter Property="Content">
                                            <Setter.Value>
                                                <TextBlock
                                                        Text="{StaticResource GlyphArrow60Left}"
                                                        FontFamily="{StaticResource TelerikWebUI}"
                                                        Foreground="{Binding (TextElement.Foreground), RelativeSource={RelativeSource AncestorType=ContentPresenter}}"
                                                        FontSize="16"
                                                        FontStyle="Normal"
                                                        FontWeight="Normal"
                                                        HorizontalAlignment="Center"
                                                        VerticalAlignment="Center"/>
                                            </Setter.Value>
                                        </Setter>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </telerik:RadButton.Style>
                    </telerik:RadButton>
                    <telerik:RadButton x:Name="MoveCurrentToLast" Command="{x:Static controls:RadCollectionNavigatorCommands.MoveCurrentToLast}" Visibility="{Binding Converter={StaticResource VisibilityConverter}, RelativeSource={RelativeSource TemplatedParent}, Path=CommandButtonsVisibility, ConverterParameter=last}">
                        <ToolTipService.ToolTip>
                            <ToolTip telerik:LocalizationManager.ResourceKey="DataForm_MoveCurrentToLast"/>
                        </ToolTipService.ToolTip>
                        <telerik:RadButton.Style>
                            <Style TargetType="{x:Type telerik:RadButton}" BasedOn="{StaticResource RadCollectionNavigatorButtonStyle}">
                                <Style.Triggers>
                                    <Trigger Property="FlowDirection" Value="LeftToRight">
                                        <Setter Property="Content">
                                            <Setter.Value>
                                                <TextBlock
                                                        Text="{StaticResource GlyphArrowForward}"
                                                        FontFamily="{StaticResource TelerikWebUI}"
                                                        Foreground="{Binding (TextElement.Foreground), RelativeSource={RelativeSource AncestorType=ContentPresenter}}"
                                                        FontSize="16"
                                                        FontStyle="Normal"
                                                        FontWeight="Normal"
                                                        HorizontalAlignment="Center"
                                                        VerticalAlignment="Center"/>
                                            </Setter.Value>
                                        </Setter>
                                    </Trigger>
                                    <Trigger Property="FlowDirection" Value="RightToLeft">
                                        <Setter Property="Content">
                                            <Setter.Value>
                                                <TextBlock
                                                        Text="{StaticResource GlyphArrowBackward}"
                                                        FontFamily="{StaticResource TelerikWebUI}"
                                                        Foreground="{Binding (TextElement.Foreground), RelativeSource={RelativeSource AncestorType=ContentPresenter}}"
                                                        FontSize="16"
                                                        FontStyle="Normal"
                                                        FontWeight="Normal"
                                                        HorizontalAlignment="Center"
                                                        VerticalAlignment="Center"/>
                                            </Setter.Value>
                                        </Setter>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </telerik:RadButton.Style>
                    </telerik:RadButton>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Grid.Column="1" HorizontalAlignment="Right">
                    <telerik:RadButton x:Name="AddNew"
                            Style="{StaticResource RadCollectionNavigatorButtonStyle}"
                            Command="{x:Static controls:RadCollectionNavigatorCommands.AddNew}"
                            Visibility="{Binding Converter={StaticResource VisibilityConverter}, RelativeSource={RelativeSource TemplatedParent}, Path=CommandButtonsVisibility, ConverterParameter=add}">
                        <ToolTipService.ToolTip>
                            <ToolTip telerik:LocalizationManager.ResourceKey="DataForm_AddNew"/>
                        </ToolTipService.ToolTip>
                        <TextBlock
                                Text="{StaticResource GlyphFile}"
                                FontFamily="{StaticResource TelerikWebUI}"
                                Foreground="{Binding (TextElement.Foreground), RelativeSource={RelativeSource AncestorType=ContentPresenter}}"
                                FontSize="16"
                                FontStyle="Normal"
                                FontWeight="Normal"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"/>
                    </telerik:RadButton>
                    <telerik:RadButton x:Name="BeginEdit"
                            Style="{StaticResource RadCollectionNavigatorButtonStyle}"
                            Command="{x:Static controls:RadCollectionNavigatorCommands.BeginEdit}"
                            Visibility="{Binding Converter={StaticResource VisibilityConverter}, RelativeSource={RelativeSource TemplatedParent}, Path=CommandButtonsVisibility, ConverterParameter=edit}">
                        <ToolTipService.ToolTip>
                            <ToolTip telerik:LocalizationManager.ResourceKey="DataForm_BeginEdit"/>
                        </ToolTipService.ToolTip>
                        <TextBlock
                                Text="{StaticResource GlyphEdit}"
                                FontFamily="{StaticResource TelerikWebUI}"
                                Foreground="{Binding (TextElement.Foreground), RelativeSource={RelativeSource AncestorType=ContentPresenter}}"
                                FontSize="16"
                                FontStyle="Normal"
                                FontWeight="Normal"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"/>
                    </telerik:RadButton>
                    <telerik:RadButton x:Name="Delete"
                            Style="{StaticResource RadCollectionNavigatorButtonStyle}"
                            Visibility="{Binding Converter={StaticResource VisibilityConverter}, RelativeSource={RelativeSource TemplatedParent}, Path=CommandButtonsVisibility, ConverterParameter=delete}"
                            Command="{x:Static controls:RadCollectionNavigatorCommands.Delete}"
                            Margin="0 0 4 0">
                        <ToolTipService.ToolTip>
                            <ToolTip telerik:LocalizationManager.ResourceKey="DataForm_Delete"/>
                        </ToolTipService.ToolTip>
                        <TextBlock
                                Text="{StaticResource GlyphDelete}"
                                FontFamily="{StaticResource TelerikWebUI}"
                                Foreground="{Binding (TextElement.Foreground), RelativeSource={RelativeSource AncestorType=ContentPresenter}}"
                                FontSize="16"
                                FontStyle="Normal"
                                FontWeight="Normal"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"/>
                    </telerik:RadButton>
                </StackPanel>
            </Grid>
        </Border>
    </ControlTemplate>
    <Style x:Key="RadCollectionNavigatorStyle" TargetType="controls:RadCollectionNavigator">
        <Setter Property="Template" Value="{StaticResource Collection_NavigatorTemplate}"/>
        <Setter Property="Background" Value="{telerik1:Office2016TouchResource ResourceKey=MainBrush}"/>
        <Setter Property="BorderBrush" Value="{telerik1:Office2016TouchResource ResourceKey=BasicBrush}"/>
        <Setter Property="Foreground" Value="{telerik1:Office2016TouchResource ResourceKey=IconBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="MinHeight" Value="40"/>
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
    </Style>
    <Style TargetType="controls:RadCollectionNavigator" BasedOn="{StaticResource RadCollectionNavigatorStyle}"/>
    <ControlTemplate x:Key="ModalEditor_Template" TargetType="propertyGrid:ModalEditor">
        <Border
                Background="{TemplateBinding Background}"
                BorderBrush="{TemplateBinding BorderBrush}"
                BorderThickness="{TemplateBinding BorderThickness}"
                CornerRadius="{telerik1:Office2016TouchResource ResourceKey=CornerRadius}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="PART_ModalEditorTextBlock" Text="{Binding SourceProperty, RelativeSource={RelativeSource TemplatedParent}}" Margin="4 0 0 0" VerticalAlignment="Center"/>
                <telerik:RadButton x:Name="PART_ShowEditorButton" Grid.Column="1" Margin="1" Padding="0" Command="{x:Static propertyGrid:ModalEditor.ShowEditorCommand}" Width="36" Height="32">
                    <TextBlock
                            Text="{StaticResource GlyphEdit}"
                            FontFamily="{StaticResource TelerikWebUI}"
                            Foreground="{Binding (TextElement.Foreground), RelativeSource={RelativeSource AncestorType=ContentPresenter}}"
                            FontSize="16"
                            FontStyle="Normal"
                            FontWeight="Normal"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"/>
                </telerik:RadButton>
            </Grid>
        </Border>
    </ControlTemplate>
    <Style x:Key="ModalEditorStyle" TargetType="propertyGrid:ModalEditor">
        <Setter Property="Template" Value="{StaticResource ModalEditor_Template}"/>
        <Setter Property="Foreground" Value="{telerik1:Office2016TouchResource ResourceKey=MarkerBrush}"/>
        <Setter Property="Background" Value="{telerik1:Office2016TouchResource ResourceKey=MainBrush}"/>
        <Setter Property="BorderBrush" Value="{telerik1:Office2016TouchResource ResourceKey=BasicBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="IsTabStop" Value="False"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
    </Style>
    <Style TargetType="propertyGrid:ModalEditor" BasedOn="{StaticResource ModalEditorStyle}"/>
    <ControlTemplate x:Key="DropDownEditor_Template" TargetType="propertyGrid:DropDownEditor">
        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="PART_DropDownEditorTextBlock" Text="{Binding SourceProperty, RelativeSource={RelativeSource TemplatedParent}}" Margin="4 0 0 0" VerticalAlignment="Center"/>
                <telerik:RadDropDownButton x:Name="PART_DropDownEditorButton"
                        IsEnabled="{TemplateBinding IsEnabled}"
                        Grid.Column="1"
                        Margin="0 -1 -1 -1"
                        Padding="0"
                        DropDownContent="{Binding Editor, RelativeSource={RelativeSource TemplatedParent}}"
                        PopupPlacementTarget="{Binding RelativeSource={RelativeSource TemplatedParent}}"
                        MinHeight="32"/>
            </Grid>
        </Border>
    </ControlTemplate>
    <Style x:Key="DropDownEditorStyle" TargetType="propertyGrid:DropDownEditor">
        <Setter Property="Template" Value="{StaticResource DropDownEditor_Template}"/>
        <Setter Property="Foreground" Value="{telerik1:Office2016TouchResource ResourceKey=MarkerBrush}"/>
        <Setter Property="Background" Value="{telerik1:Office2016TouchResource ResourceKey=MainBrush}"/>
        <Setter Property="BorderBrush" Value="{telerik1:Office2016TouchResource ResourceKey=BasicBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="IsTabStop" Value="False"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
    </Style>
    <ControlTemplate x:Key="ToggleButtonTemplate" TargetType="telerik:RadToggleButton">
        <Border Background="Transparent" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
            <Border
                    HorizontalAlignment="Center"
                    BorderThickness="1"
                    VerticalAlignment="Center"
                    Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}">
                <Grid>
                    <TextBlock x:Name="Plus"
                            Text="{StaticResource GlyphPlus}"
                            FontFamily="{StaticResource TelerikWebUI}"
                            Foreground="{TemplateBinding Foreground}"
                            FontSize="16"
                            FontStyle="Normal"
                            FontWeight="Normal"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"/>
                    <TextBlock x:Name="Minus"
                            Text="{StaticResource GlyphMinus}"
                            FontFamily="{StaticResource TelerikWebUI}"
                            Foreground="{TemplateBinding Foreground}"
                            FontSize="16"
                            FontStyle="Normal"
                            FontWeight="Normal"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Opacity="0"/>
                </Grid>
            </Border>
        </Border>
        <ControlTemplate.Triggers>
            <Trigger Property="IsChecked" Value="True">
                <Setter TargetName="Plus" Property="Opacity" Value="0"/>
                <Setter TargetName="Minus" Property="Opacity" Value="1"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>
    <Style x:Key="PropertyGridNestedToggleButtonStyle" TargetType="telerik:RadToggleButton">
        <Setter Property="Template" Value="{StaticResource ToggleButtonTemplate}"/>
        <Setter Property="Foreground" Value="{telerik1:Office2016TouchResource ResourceKey=IconBrush}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="Width" Value="30"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
    </Style>
    <ControlTemplate TargetType="telerik:RadToggleButton" x:Key="PropertyGridToggleButtonTemplate">
        <Grid UseLayoutRounding="True">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Border x:Name="Arrow"
                    Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="0 1"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Stretch"
                    Width="31"
                    Margin="0 -1 0 0">
                <TextBlock x:Name="Glyph"
                        Grid.Column="0"
                        Text="{StaticResource GlyphArrow60Down}"
                        FontFamily="{StaticResource TelerikWebUI}"
                        Foreground="{telerik1:Office2016TouchResource ResourceKey=IconBrush}"
                        FontSize="16"
                        FontStyle="Normal"
                        FontWeight="Normal"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"/>
            </Border>
            <Border
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}"
                    Background="{TemplateBinding Background}"
                    Grid.ColumnSpan="2"
                    Grid.Column="1">
                <ContentControl x:Name="PART_ContentPresenter"
                        ContentTemplate="{TemplateBinding ContentTemplate}"
                        Foreground="{TemplateBinding Foreground}"
                        Content="{TemplateBinding Content}"
                        HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                        Margin="{TemplateBinding Padding}"
                        VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
            </Border>
            <Border x:Name="ExpandBorder"
                    Grid.ColumnSpan="2"
                    Grid.Column="1"
                    Opacity="0"
                    Margin="-2 -1 0 0"
                    Background="Transparent"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}"/>
        </Grid>
        <ControlTemplate.Triggers>
            <Trigger Property="IsChecked" Value="True">
                <Setter TargetName="Glyph" Property="Text" Value="{StaticResource GlyphArrow60Up}"/>
                <Setter TargetName="ExpandBorder" Property="Opacity" Value="1"/>
                <Setter TargetName="Arrow" Property="BorderThickness" Value="0 1 0 0"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>
    <Style TargetType="telerik:RadToggleButton" x:Key="PropertyGridToggleButtonStyle">
        <Setter Property="Template" Value="{StaticResource PropertyGridToggleButtonTemplate}"/>
        <Setter Property="Background" Value="{telerik1:Office2016TouchResource ResourceKey=AlternativeBrush}"/>
        <Setter Property="Foreground" Value="{telerik1:Office2016TouchResource ResourceKey=MarkerBrush}"/>
        <Setter Property="BorderBrush" Value="{telerik1:Office2016TouchResource ResourceKey=BasicBrush}"/>
        <Setter Property="BorderThickness" Value="0 0 0 1"/>
        <Setter Property="Padding" Value="0 0 0 2"/>
        <Setter Property="MinHeight" Value="35"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
    </Style>
    <Style x:Key="ItemsControlStyle" TargetType="ItemsControl">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ItemsControl">
                    <ScrollViewer Background="{x:Null}" BorderThickness="0" Margin="0 -1 0 0" FocusVisualStyle="{x:Null}">
                        <ItemsPresenter/>
                    </ScrollViewer>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style x:Key="VirtualizedItemsControlStyle" BasedOn="{StaticResource ItemsControlStyle}" TargetType="ItemsControl">
        <Setter Property="ItemsPanel">
            <Setter.Value>
                <ItemsPanelTemplate>
                    <VirtualizingStackPanel/>
                </ItemsPanelTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="ScrollViewer.CanContentScroll" Value="True"/>
    </Style>
    <DataTemplate x:Key="flatTemplate">
        <ItemsControl ItemsSource="{Binding Converter={StaticResource FlatItemSourceConverter}}" Style="{StaticResource ItemsControlStyle}" IsTabStop="False">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <propertyGrid:PropertyGridField Content="{Binding}" Visibility="{Binding Visibility}"/>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </DataTemplate>
    <DataTemplate x:Key="virtualizedFlatTemplate">
        <ItemsControl ItemsSource="{Binding Converter={StaticResource FlatItemSourceConverter}}" Style="{StaticResource VirtualizedItemsControlStyle}" IsTabStop="False">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <propertyGrid:PropertyGridField Content="{Binding}" Visibility="{Binding Visibility}"/>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </DataTemplate>
    <DataTemplate x:Key="flatRenderingTemplate">
        <ItemsControl VirtualizingStackPanel.VirtualizationMode="Standard" ItemsSource="{Binding}" Style="{StaticResource VirtualizedItemsControlStyle}" IsTabStop="False">
            <ItemsControl.ItemTemplateSelector>
                <propertyGrid:FlatRenderingItemTemplateSelector>
                    <propertyGrid:FlatRenderingItemTemplateSelector.FieldTemplate>
                        <DataTemplate>
                            <propertyGrid:PropertyGridField Content="{Binding}" Visibility="{Binding Visibility}"/>
                        </DataTemplate>
                    </propertyGrid:FlatRenderingItemTemplateSelector.FieldTemplate>
                    <propertyGrid:FlatRenderingItemTemplateSelector.GroupTemplate>
                        <DataTemplate>
                            <telerik:RadToggleButton x:Name="expandCollapseButton" Content="{Binding DisplayName}" Style="{Binding GroupContainerStyle}" IsChecked="{Binding IsExpanded, Mode=TwoWay}"/>
                        </DataTemplate>
                    </propertyGrid:FlatRenderingItemTemplateSelector.GroupTemplate>
                </propertyGrid:FlatRenderingItemTemplateSelector>
            </ItemsControl.ItemTemplateSelector>
        </ItemsControl>
    </DataTemplate>
    <propertyGrid:GroupingItemsSourceConverter x:Key="GroupingItemsSourceConverter"/>
    <DataTemplate x:Key="groupTemplate">
        <ItemsControl ItemsSource="{Binding ., Converter={StaticResource GroupingItemsSourceConverter}}" Style="{StaticResource ItemsControlStyle}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border BorderThickness="0 1 0 0" BorderBrush="{telerik1:Office2016TouchResource ResourceKey=BasicBrush}" Margin="0 -1 0 0">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <telerik:RadToggleButton x:Name="expandCollapseButton" Content="{Binding Key}" IsChecked="True" Style="{StaticResource PropertyGridToggleButtonStyle}"/>
                            <ContentPresenter x:Name="PART_ContentPresenter"
                                    Content="{Binding Items}"
                                    Grid.Row="1"
                                    Visibility="{Binding IsChecked, Converter={StaticResource BooleanToVisibilityConverter}, ElementName=expandCollapseButton}">
                                <ContentPresenter.ContentTemplate>
                                    <DataTemplate>
                                        <ItemsControl ItemsSource="{Binding Converter={StaticResource FlatItemSourceConverter}}" IsTabStop="False">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <propertyGrid:PropertyGridField Content="{Binding}" Visibility="{Binding Visibility}"/>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </DataTemplate>
                                </ContentPresenter.ContentTemplate>
                            </ContentPresenter>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </DataTemplate>
    <propertyGrid:ItemTemplateConverter x:Key="ItemTemplateConverter"
            FlatTemplate="{StaticResource flatTemplate}"
            GroupTemplate="{StaticResource groupTemplate}"
            VirtualizedFlatTemplate="{StaticResource virtualizedFlatTemplate}"
            FlatRenderingTemplate="{StaticResource flatRenderingTemplate}"/>
    <Style x:Key="PropertyGridGripperStyle" TargetType="Thumb">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="Width" Value="8"/>
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="VerticalContentAlignment" Value="Stretch"/>
        <Setter Property="Cursor" Value="SizeWE"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Thumb">
                    <Border
                            Padding="{TemplateBinding Padding}"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <ControlTemplate x:Key="PropertyGridColumnResizerTemplate" TargetType="propertyGrid:PropertyGridColumnResizer">
        <Border VerticalAlignment="Stretch" Background="{TemplateBinding Background}">
            <Thumb HorizontalAlignment="Right" Style="{StaticResource PropertyGridGripperStyle}"/>
        </Border>
    </ControlTemplate>
    <Style x:Key="PropertyGridColumnResizerStyle" TargetType="propertyGrid:PropertyGridColumnResizer">
        <Setter Property="Template" Value="{StaticResource PropertyGridColumnResizerTemplate}"/>
        <Setter Property="IsTabStop" Value="False"/>
    </Style>
    <ControlTemplate x:Key="PropertyGridFieldTemplate" TargetType="propertyGrid:PropertyGridField">
        <Border x:Name="PART_RootElement" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid x:Name="PART_PropertyGridFieldGridN" Height="{TemplateBinding Height}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="{Binding LabelColumnWidth, ElementName=PropertyGridPresenter}"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Rectangle x:Name="Background_Visual"
                            Fill="{telerik1:Office2016TouchResource ResourceKey=MainBrush}"
                            Grid.Column="1"
                            Grid.ColumnSpan="2"
                            HorizontalAlignment="Stretch"
                            VerticalAlignment="Stretch"/>
                    <Rectangle x:Name="Background_Over"
                            Grid.Column="1"
                            Grid.ColumnSpan="2"
                            Fill="{telerik1:Office2016TouchResource ResourceKey=AccentMouseOverBrush}"
                            HorizontalAlignment="Stretch"
                            Visibility="{Binding IsMouseOver, Converter={StaticResource BooleanToVisibilityConverter}, RelativeSource={RelativeSource TemplatedParent}}"
                            VerticalAlignment="Stretch"/>
                    <Rectangle x:Name="PART_SelectionBackgroundN"
                            Grid.Column="1"
                            Visibility="Collapsed"
                            Fill="{TemplateBinding BorderBrush}"
                            HorizontalAlignment="Stretch"
                            VerticalAlignment="Stretch"/>
                    <Rectangle x:Name="Background_Selected"
                            Grid.Column="1"
                            Grid.ColumnSpan="2"
                            Fill="{telerik1:Office2016TouchResource ResourceKey=AccentBrush}"
                            HorizontalAlignment="Stretch"
                            Visibility="{Binding IsSelected, Converter={StaticResource BooleanToVisibilityConverter}, RelativeSource={RelativeSource TemplatedParent}}"
                            VerticalAlignment="Stretch"/>
                    <Grid x:Name="FieldIndicator" Visibility="{Binding RelativeSource={RelativeSource AncestorType={x:Type controls:RadPropertyGrid}}, Path=FieldIndicatorVisibility}">
                        <Border BorderThickness="0 0 1 0" BorderBrush="{TemplateBinding BorderBrush}" Width="30" Background="{telerik1:Office2016TouchResource ResourceKey=AlternativeBrush}">
                            <TextBlock x:Name="NavigatorIndicator"
                                    Text="{StaticResource GlyphArrowChevronRight}"
                                    FontFamily="{StaticResource TelerikWebUI}"
                                    Foreground="{telerik1:Office2016TouchResource ResourceKey=IconBrush}"
                                    FontSize="16"
                                    FontStyle="Normal"
                                    FontWeight="Normal"
                                    Visibility="Collapsed"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"/>
                        </Border>
                    </Grid>
                    <Border Grid.Column="1" BorderThickness="0 0 1 1" BorderBrush="{TemplateBinding BorderBrush}">
                        <StackPanel Orientation="Horizontal">
                            <propertyGrid:PropertyGridIndentPresenter IndentLevel="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=IndentLevel}">
                                <propertyGrid:PropertyGridIndentPresenter.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <StackPanel Orientation="Horizontal"/>
                                    </ItemsPanelTemplate>
                                </propertyGrid:PropertyGridIndentPresenter.ItemsPanel>
                                <propertyGrid:PropertyGridIndentPresenter.ItemTemplate>
                                    <DataTemplate>
                                        <propertyGrid:PropertyGridIndentCell/>
                                    </DataTemplate>
                                </propertyGrid:PropertyGridIndentPresenter.ItemTemplate>
                                <propertyGrid:PropertyGridIndentPresenter.Template>
                                    <ControlTemplate>
                                        <ItemsPresenter/>
                                    </ControlTemplate>
                                </propertyGrid:PropertyGridIndentPresenter.Template>
                            </propertyGrid:PropertyGridIndentPresenter>
                            <telerik:RadToggleButton x:Name="PART_NestedPropertiesButton"
                                    Style="{StaticResource PropertyGridNestedToggleButtonStyle}"
                                    Visibility="{Binding ShouldDisplayNestedProperties, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource BooleanToVisibilityConverter}}"
                                    IsChecked="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=IsExpanded, Mode=TwoWay}"/>
                            <TextBlock x:Name="PART_FieldLabelN" Text="{Binding DisplayName}" IsHitTestVisible="True" VerticalAlignment="Center" Margin="4 2"/>
                        </StackPanel>
                    </Border>
                    <Border x:Name="BorderVisual" Grid.Column="2" BorderThickness="0 0 0 1" BorderBrush="{TemplateBinding BorderBrush}"/>
                    <ContentControl x:Name="Content"
                            Content="{TemplateBinding Content}"
                            MinHeight="22"
                            Margin="1 1 2 2"
                            ContentTemplate="{TemplateBinding ContentTemplate}"
                            Grid.Column="2"
                            HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                            VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"/>
                    <propertyGrid:PropertyGridColumnResizer x:Name="ColumnResizer" Grid.Column="1" Margin="0 0 -4 0" HorizontalAlignment="Right"/>
                </Grid>
                <ItemsControl Visibility="{Binding RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource BooleanToVisibilityConverter}, Path=ShouldDisplayNestedItemsControl }" Grid.Row="1" ItemsSource="{Binding NestedProperties, Converter={StaticResource FlatItemSourceConverter}}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <propertyGrid:PropertyGridField Content="{Binding}" Visibility="{Binding Visibility}"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <VirtualizingStackPanel/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                </ItemsControl>
            </Grid>
        </Border>
        <ControlTemplate.Triggers>
            <Trigger Property="IsSelected" Value="False">
                <Setter TargetName="PART_FieldLabelN" Property="Foreground" Value="{telerik1:Office2016TouchResource ResourceKey=MarkerBrush}"/>
                <Setter TargetName="PART_SelectionBackgroundN" Property="Visibility" Value="Collapsed"/>
                <Setter TargetName="PART_NestedPropertiesButton" Property="Foreground" Value="{telerik1:Office2016TouchResource ResourceKey=IconBrush}"/>
            </Trigger>
            <Trigger Property="IsSelected" Value="True">
                <Setter TargetName="PART_FieldLabelN" Property="Foreground" Value="{telerik1:Office2016TouchResource ResourceKey=MarkerInvertedBrush}"/>
                <Setter TargetName="PART_NestedPropertiesButton" Property="Foreground" Value="{telerik1:Office2016TouchResource ResourceKey=MarkerInvertedBrush}"/>
                <Setter Property="Foreground" Value="{telerik1:Office2016TouchResource ResourceKey=MarkerInvertedBrush}"/>
                <Setter TargetName="PART_SelectionBackgroundN" Property="Visibility" Value="Visible"/>
            </Trigger>
            <Trigger Property="IsCurrent" Value="True">
                <Setter TargetName="NavigatorIndicator" Property="Visibility" Value="Visible"/>
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Foreground" Value="{telerik1:Office2016TouchResource ResourceKey=MarkerInvertedBrush}"/>
                <Setter TargetName="PART_FieldLabelN" Property="Foreground" Value="{telerik1:Office2016TouchResource ResourceKey=MarkerInvertedBrush}"/>
                <Setter TargetName="PART_NestedPropertiesButton" Property="Foreground" Value="{telerik1:Office2016TouchResource ResourceKey=MarkerInvertedBrush}"/>
            </Trigger>
            <DataTrigger Binding="{Binding ComputedVerticalScrollBarVisibility, RelativeSource={RelativeSource AncestorType={x:Type ScrollViewer}}}" Value="Visible">
                <Setter TargetName="BorderVisual" Property="BorderThickness" Value="0 0 1 1"/>
            </DataTrigger>
            <Trigger Property="FlowDirection" Value="RightToLeft">
                <Setter TargetName="NavigatorIndicator" Property="Text" Value="{StaticResource GlyphArrowChevronLeft}"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>
    <ControlTemplate x:Key="PropertyGridValidationTemplate">
        <StackPanel Orientation="Horizontal">
            <Grid>
                <Border Background="{telerik1:Office2016TouchResource ResourceKey=ValidationBrush}" Margin="0 0 2 0" MinHeight="32">
                    <TextBlock x:Name="PART_ToolTip"
                            FontSize="{telerik1:Office2016TouchResource ResourceKey=FontSize}"
                            FontFamily="{telerik1:Office2016TouchResource ResourceKey=FontFamily}"
                            Foreground="{telerik1:Office2016TouchResource ResourceKey=MarkerInvertedBrush}"
                            VerticalAlignment="Center"
                            Padding="4 6 4 5"
                            Text="{Binding ElementName=Holder, Path=AdornedElement.(Validation.Errors).CurrentItem.ErrorContent}"/>
                </Border>
            </Grid>
            <Grid SnapsToDevicePixels="True" VerticalAlignment="Top">
                <Border Background="Transparent" HorizontalAlignment="Right" VerticalAlignment="Top" Width="3" Height="3"/>
                <AdornedElementPlaceholder x:Name="Holder"/>
                <Border BorderBrush="{telerik1:Office2016TouchResource ResourceKey=ValidationBrush}" BorderThickness="1" CornerRadius="{telerik1:Office2016TouchResource ResourceKey=CornerRadius}"/>
            </Grid>
        </StackPanel>
    </ControlTemplate>
    <Style x:Key="PropertyGridFieldStyle" TargetType="propertyGrid:PropertyGridField">
        <Setter Property="Template" Value="{StaticResource PropertyGridFieldTemplate}"/>
        <Setter Property="FontFamily" Value="{telerik1:Office2016TouchResource ResourceKey=FontFamily}"/>
        <Setter Property="FontSize" Value="{telerik1:Office2016TouchResource ResourceKey=FontSize}"/>
        <Setter Property="Foreground" Value="{telerik1:Office2016TouchResource ResourceKey=MarkerBrush}"/>
        <Setter Property="BorderBrush" Value="{telerik1:Office2016TouchResource ResourceKey=BasicBrush}"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="MinHeight" Value="35"/>
        <Setter Property="IsTabStop" Value="False"/>
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="VerticalContentAlignment" Value="Stretch"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Style.Resources>
            <Style TargetType="TextBlock">
                <Style.Triggers>
                    <Trigger Property="IsEnabled" Value="False">
                        <Setter Property="Opacity" Value="{telerik1:Office2016TouchResource ResourceKey=DisabledOpacity}"/>
                    </Trigger>
                </Style.Triggers>
            </Style>
            <Style TargetType="TextBox" BasedOn="{StaticResource TextBoxStyle}">
                <Setter Property="Validation.ErrorTemplate" Value="{StaticResource PropertyGridValidationTemplate}"/>
            </Style>
        </Style.Resources>
    </Style>
    <propertyGrid:VisibilityToGridLenghtConverter x:Key="VisibilityToGridLenghtConverter"/>
    <ControlTemplate x:Key="RadPropertyGridTemplate" TargetType="controls:RadPropertyGrid">
        <Grid x:Name="PART_PropertyGridContainer">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="{Binding DescriptionPanelVisibility, Converter={StaticResource VisibilityToGridLenghtConverter}, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"/>
            </Grid.RowDefinitions>
            <Border x:Name="PART_RootElement"
                    Margin="-1"
                    Background="{TemplateBinding Background}"
                    Grid.RowSpan="3"
                    BorderThickness="{TemplateBinding BorderThickness}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    CornerRadius="{telerik1:Office2016TouchResource ResourceKey=CornerRadius}"/>
            <Border x:Name="Header"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    Background="{TemplateBinding Background}"
                    Visibility="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}, Converter={StaticResource HeaderVisibilityConverter}}"
                    Panel.ZIndex="100"
                    BorderThickness="0 0 0 1"
                    Margin="0 0 0 1"
                    MinHeight="40">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <telerik:RadRadioButton x:Name="groupButton"
                            Visibility="{TemplateBinding SortAndGroupButtonsVisibility}"
                            IsChecked="{Binding Path=IsGrouped, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"
                            VerticalAlignment="Center"
                            MinHeight="32"
                            MinWidth="36"
                            Margin="4 1">
                        <ToolTipService.ToolTip>
                            <ToolTip telerik:LocalizationManager.ResourceKey="Categorize"/>
                        </ToolTipService.ToolTip>
                        <TextBlock
                                Text="{StaticResource GlyphCategorize}"
                                FontFamily="{StaticResource TelerikWebUI}"
                                Foreground="{Binding (TextElement.Foreground), RelativeSource={RelativeSource AncestorType=ContentPresenter}}"
                                FontSize="16"
                                FontStyle="Normal"
                                FontWeight="Normal"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"/>
                    </telerik:RadRadioButton>
                    <telerik:RadRadioButton x:Name="sortButton"
                            Content="A-Z"
                            Visibility="{TemplateBinding SortAndGroupButtonsVisibility}"
                            Grid.Column="1"
                            Padding="4 0"
                            VerticalAlignment="Center"
                            MinHeight="32"
                            MinWidth="36"
                            Margin="0 1">
                        <ToolTipService.ToolTip>
                            <ToolTip telerik:LocalizationManager.ResourceKey="Alphabetical"/>
                        </ToolTipService.ToolTip>
                    </telerik:RadRadioButton>
                    <TextBox x:Name="searchAsYouTypeTextBox"
                            Visibility="{TemplateBinding SearchBoxVisibility}"
                            Grid.Column="2"
                            HorizontalAlignment="Stretch"
                            Margin="4 3"
                            Padding="4 2 18 2"
                            VerticalAlignment="Center"
                            Text="{Binding FilterText, ElementName=PropertyGridPresenter, Mode=TwoWay}"
                            telerik:TextBoxBehavior.SelectAllOnGotFocus="True"
                            telerik:TextBoxBehavior.UpdateTextOnEnter="True"
                            telerik:TextBoxBehavior.UpdateTextOnTextChanged="True"
                            MinHeight="32"/>
                    <Border Grid.Column="2" Width="18" HorizontalAlignment="Right" Visibility="{TemplateBinding SearchBoxVisibility}" Margin="0 0 5 0" VerticalAlignment="Center">
                        <TextBlock
                                Text="{StaticResource GlyphZoom}"
                                FontFamily="{StaticResource TelerikWebUI}"
                                Foreground="{telerik1:Office2016TouchResource ResourceKey=IconBrush}"
                                FontSize="16"
                                FontStyle="Normal"
                                FontWeight="Normal"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Margin="0 0 2 0"/>
                    </Border>
                </Grid>
            </Border>
            <propertyGrid:PropertyGridPresenter x:Name="PropertyGridPresenter"
                    Grid.Row="1"
                    LabelColumnWidth="{Binding LabelColumnWidth, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"
                    PropertyDefinitions="{Binding PropertyDefinitions, RelativeSource={RelativeSource TemplatedParent}}"
                    SearchInNestedProperties="{Binding SearchInNestedProperties, RelativeSource={RelativeSource TemplatedParent}}"
                    ContentTemplate="{Binding Path=PropertyGridLayoutState, RelativeSource={RelativeSource Self}, Converter={StaticResource ItemTemplateConverter}}"
                    IsGrouped="{TemplateBinding IsGrouped}"
                    RenderMode="{TemplateBinding RenderMode}"/>
            <Border x:Name="DescriptionPanel"
                    Grid.Row="2"
                    MinHeight="88"
                    BorderThickness="0 1 0 0"
                    Margin="0 1 0 0"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    Visibility="{TemplateBinding DescriptionPanelVisibility}">
                <StackPanel x:Name="PART_DescriptionPanel" Orientation="Vertical">
                    <TextBlock x:Name="PART_DescriptionLabelTextBlock"
                            Text="{Binding SelectedPropertyDefinition.DisplayName,ElementName=PropertyGridPresenter}"
                            Margin="9 10 20 5"
                            FontWeight="Bold"/>
                    <TextBlock x:Name="PART_DescriptionContentTextBlock"
                            Text="{Binding SelectedPropertyDefinition.Description,ElementName=PropertyGridPresenter}"
                            Margin="9 0 20 5"
                            TextWrapping="Wrap"/>
                </StackPanel>
            </Border>
            <GridSplitter x:Name="PART_DescriptionPanelSplitter"
                    Grid.Row="2"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Top"
                    Background="Transparent"
                    Visibility="{Binding CanUserResizeDescriptionPanel, Converter={StaticResource BooleanToVisibilityConverter}, RelativeSource={RelativeSource TemplatedParent}}"
                    Height="3"/>
        </Grid>
    </ControlTemplate>
    <Style x:Key="RadPropertyGridStyle" TargetType="controls:RadPropertyGrid">
        <Setter Property="Template" Value="{StaticResource RadPropertyGridTemplate}"/>
        <Setter Property="FontFamily" Value="{telerik1:Office2016TouchResource ResourceKey=FontFamily}"/>
        <Setter Property="FontSize" Value="{telerik1:Office2016TouchResource ResourceKey=FontSize}"/>
        <Setter Property="Foreground" Value="{telerik1:Office2016TouchResource ResourceKey=MarkerBrush}"/>
        <Setter Property="Background" Value="{telerik1:Office2016TouchResource ResourceKey=AlternativeBrush}"/>
        <Setter Property="BorderBrush" Value="{telerik1:Office2016TouchResource ResourceKey=BasicBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="GroupStyle" Value="{StaticResource PropertyGridToggleButtonStyle}"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
    </Style>
    <Style x:Key="PropertyGridIndentCellStyle" TargetType="propertyGrid:PropertyGridIndentCell">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="VerticalContentAlignment" Value="Stretch"/>
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="Width" Value="30"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="propertyGrid:PropertyGridIndentCell">
                    <Border
                            Background="{TemplateBinding Background}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Margin="{TemplateBinding Padding}"
                            BorderBrush="{TemplateBinding BorderBrush}"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style TargetType="controls:RadPropertyGrid" BasedOn="{StaticResource RadPropertyGridStyle}"/>
    <Style TargetType="propertyGrid:PropertyGridField" BasedOn="{StaticResource PropertyGridFieldStyle}"/>
    <Style TargetType="propertyGrid:PropertyGridIndentCell" BasedOn="{StaticResource PropertyGridIndentCellStyle}"/>
    <Style TargetType="propertyGrid:PropertyGridColumnResizer" BasedOn="{StaticResource PropertyGridColumnResizerStyle}"/>
    <Style TargetType="propertyGrid:DropDownEditor" BasedOn="{StaticResource DropDownEditorStyle}"/>
    <sys:Double x:Key="HeaderNavigationButtonWidthHeight">30</sys:Double>
    <Style x:Key="CollectionEditorGripperStyle" TargetType="Thumb">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="Width" Value="20"/>
        <Setter Property="HorizontalAlignment" Value="Right"/>
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="VerticalContentAlignment" Value="Stretch"/>
        <Setter Property="Cursor" Value="SizeNWSE"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Thumb">
                    <Border
                            Padding="{TemplateBinding Padding}"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <ControlTemplate x:Key="CollectionEditor_Template" TargetType="propertyGrid:CollectionEditor">
        <Border x:Name="PART_RootBorder"
                Height="{Binding RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay, Path=Height}"
                Width="{Binding RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay, Path=Width}"
                BorderBrush="{TemplateBinding BorderBrush}"
                BorderThickness="{TemplateBinding BorderThickness}"
                Background="{TemplateBinding Background}">
            <Grid Background="Transparent">
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="3*"/>
                </Grid.ColumnDefinitions>
                <Border x:Name="Header"
                        MinHeight="18"
                        Grid.ColumnSpan="2"
                        Visibility="{Binding Header, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource DescriptionVisibilityConverter}}"
                        BorderThickness="0 0 0 1"
                        BorderBrush="{TemplateBinding BorderBrush}">
                    <ContentControl
                            Margin="{TemplateBinding Padding}"
                            Content="{TemplateBinding Header}"
                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                            HorizontalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                </Border>
                <Grid Grid.Row="1">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Border BorderThickness="0 0 0 1" MinHeight="18" BorderBrush="{TemplateBinding BorderBrush}">
                        <Grid MaxHeight="38">
                            <TextBlock telerik:LocalizationManager.ResourceKey="CollectionEditorItems" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                <telerik:RadButton x:Name="moveDownButton"
                                        IsEnabled="{TemplateBinding IsEnabled}"
                                        telerik:TouchManager.TouchMode="None"
                                        Command="{x:Static propertyGrid:CollectionEditorCommands.MoveCurrentToNext}"
                                        MinWidth="{StaticResource HeaderNavigationButtonWidthHeight}"
                                        MinHeight="{StaticResource HeaderNavigationButtonWidthHeight}"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        IsBackgroundVisible="False">
                                    <TextBlock
                                            Text="{StaticResource GlyphArrow60Down}"
                                            FontFamily="{StaticResource TelerikWebUI}"
                                            Foreground="{Binding (TextElement.Foreground), RelativeSource={RelativeSource AncestorType=ContentPresenter}}"
                                            FontSize="16"
                                            FontStyle="Normal"
                                            FontWeight="Normal"
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Center"/>
                                </telerik:RadButton>
                                <telerik:RadButton x:Name="moveUpButton"
                                        IsEnabled="{TemplateBinding IsEnabled}"
                                        telerik:TouchManager.TouchMode="None"
                                        Command="{x:Static propertyGrid:CollectionEditorCommands.MoveCurrentToPrevious}"
                                        MinWidth="{StaticResource HeaderNavigationButtonWidthHeight}"
                                        MinHeight="{StaticResource HeaderNavigationButtonWidthHeight}"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        Margin="0 0 4 0"
                                        IsBackgroundVisible="False">
                                    <TextBlock
                                            Text="{StaticResource GlyphArrow60Up}"
                                            FontFamily="{StaticResource TelerikWebUI}"
                                            Foreground="{Binding (TextElement.Foreground), RelativeSource={RelativeSource AncestorType=ContentPresenter}}"
                                            FontSize="16"
                                            FontStyle="Normal"
                                            FontWeight="Normal"
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Center"/>
                                </telerik:RadButton>
                            </StackPanel>
                        </Grid>
                    </Border>
                    <ListBox x:Name="PART_CollectionListBox"
                            Grid.Row="1"
                            ItemTemplate="{TemplateBinding ItemTemplate}"
                            IsEnabled="{TemplateBinding IsEnabled}"
                            BorderBrush="{x:Null}"
                            BorderThickness="0"
                            ItemsSource="{TemplateBinding CollectionView}"
                            SelectedItem="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}, Path=CurrentItem, Mode=TwoWay}"/>
                </Grid>
                <Grid Grid.Row="1" Grid.Column="2">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition/>
                    </Grid.RowDefinitions>
                    <Border BorderThickness="1 0 0 1" MinHeight="18" BorderBrush="{TemplateBinding BorderBrush}" Margin="0 0 0 1" MaxHeight="39">
                        <TextBlock telerik:LocalizationManager.ResourceKey="CollectionEditorProperties" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                    </Border>
                    <controls:RadPropertyGrid
                            Grid.Row="1"
                            Margin="1 0 0 0"
                            IsReadOnly="{TemplateBinding IsReadOnly}"
                            IsEnabled="{TemplateBinding IsEnabled}"
                            Item="{TemplateBinding CurrentItem}"
                            LabelColumnWidth="100"
                            SortAndGroupButtonsVisibility="Collapsed"
                            SearchBoxVisibility="Collapsed"
                            DescriptionPanelVisibility="Collapsed"/>
                </Grid>
                <Border x:Name="FooterPanel_Background" BorderBrush="{TemplateBinding BorderBrush}" Grid.Row="2" BorderThickness="0 1 0 0" Grid.ColumnSpan="2">
                    <Grid>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="0 7">
                            <telerik:RadButton
                                    IsEnabled="{TemplateBinding IsEnabled}"
                                    telerik:TouchManager.TouchMode="None"
                                    MinWidth="50"
                                    telerik:LocalizationManager.ResourceKey="CollectionEditorAdd"
                                    Command="{x:Static propertyGrid:CollectionEditorCommands.AddNew}"
                                    Margin="7 0"/>
                            <telerik:RadButton
                                    IsEnabled="{TemplateBinding IsEnabled}"
                                    telerik:TouchManager.TouchMode="None"
                                    MinWidth="50"
                                    telerik:LocalizationManager.ResourceKey="CollectionEditorRemove"
                                    Command="{x:Static propertyGrid:CollectionEditorCommands.Delete}"/>
                        </StackPanel>
                        <Thumb x:Name="PART_ResizeThumb" Style="{StaticResource CollectionEditorGripperStyle}" Visibility="{TemplateBinding ResizeGripperVisibility}"/>
                    </Grid>
                </Border>
            </Grid>
        </Border>
    </ControlTemplate>
    <Style x:Key="CollectionEditorStyle" TargetType="propertyGrid:CollectionEditor">
        <Setter Property="Template" Value="{StaticResource CollectionEditor_Template}"/>
        <Setter Property="FontFamily" Value="{telerik1:Office2016TouchResource ResourceKey=FontFamily}"/>
        <Setter Property="FontSize" Value="{telerik1:Office2016TouchResource ResourceKey=FontSize}"/>
        <Setter Property="Foreground" Value="{telerik1:Office2016TouchResource ResourceKey=MarkerBrush}"/>
        <Setter Property="Background" Value="{telerik1:Office2016TouchResource ResourceKey=AlternativeBrush}"/>
        <Setter Property="BorderBrush" Value="{telerik1:Office2016TouchResource ResourceKey=BasicBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="8 12"/>
        <Setter Property="Width" Value="580"/>
        <Setter Property="Height" Value="404"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="IsTabStop" Value="False"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
    </Style>
    <Style TargetType="propertyGrid:CollectionEditor" BasedOn="{StaticResource CollectionEditorStyle}"/>
    <ControlTemplate x:Key="CollectionEditorPicker_Template" TargetType="propertyGrid:CollectionEditorPicker">
        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="PART_CollectionEditorTextBlock"
                        Text="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=Source}"
                        VerticalAlignment="Center"
                        Margin="{TemplateBinding Padding}"/>
                <telerik:RadDropDownButton
                        IsEnabled="{TemplateBinding IsEnabled}"
                        Grid.Column="1"
                        HorizontalContentAlignment="Left"
                        Margin="0 -1 -1 -1"
                        Padding="0"
                        PopupPlacementTarget="{Binding RelativeSource={RelativeSource TemplatedParent}}">
                    <telerik:RadDropDownButton.DropDownContent>
                        <propertyGrid:CollectionEditor x:Name="PART_CollectionEditor"
                                Source="{Binding RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay, Path=Source}"
                                Header="{Binding RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay, Path=Header}"
                                ResizeGripperVisibility="Visible"
                                IsReadOnly="{TemplateBinding IsReadOnly}"
                                Margin="-1"/>
                    </telerik:RadDropDownButton.DropDownContent>
                </telerik:RadDropDownButton>
            </Grid>
        </Border>
    </ControlTemplate>
    <Style x:Key="CollectionEditorPickerStyle" TargetType="propertyGrid:CollectionEditorPicker">
        <Setter Property="Template" Value="{StaticResource CollectionEditorPicker_Template}"/>
        <Setter Property="FontFamily" Value="{telerik1:Office2016TouchResource ResourceKey=FontFamily}"/>
        <Setter Property="FontSize" Value="{telerik1:Office2016TouchResource ResourceKey=FontSize}"/>
        <Setter Property="Foreground" Value="{telerik1:Office2016TouchResource ResourceKey=MarkerBrush}"/>
        <Setter Property="Background" Value="{telerik1:Office2016TouchResource ResourceKey=MainBrush}"/>
        <Setter Property="BorderBrush" Value="{telerik1:Office2016TouchResource ResourceKey=BasicBrush}"/>
        <Setter Property="MinHeight" Value="32"/>
        <Setter Property="Padding" Value="4 0"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="IsTabStop" Value="False"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="Validation.ErrorTemplate" Value="{StaticResource ValidationTooltipTemplate}"/>
    </Style>
    <Style TargetType="propertyGrid:CollectionEditorPicker" BasedOn="{StaticResource CollectionEditorPickerStyle}"/>
    <ControlTemplate x:Key="FlagEnumEditorNonEditableTemplate" TargetType="propertyGrid:FlagEnumEditor">
        <telerik:RadDropDownButton x:Name="PART_EnumValuesDropDownButton"
                HorizontalAlignment="Stretch"
                HorizontalContentAlignment="Stretch"
                IsTabStop="False"
                ClickMode="Press"
                Padding="0"
                Focusable="False"
                TabIndex="1"
                VerticalAlignment="Stretch"
                VerticalContentAlignment="Center">
            <telerik:RadDropDownButton.DropDownContent>
                <ScrollViewer ScrollViewer.HorizontalScrollBarVisibility="Auto" ScrollViewer.VerticalScrollBarVisibility="Auto" BorderThickness="0" Padding="2">
                    <ItemsControl ItemsSource="{Binding EnumItemsSource, RelativeSource={RelativeSource TemplatedParent}}" BorderThickness="0">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay}" Content="{Binding Value}" Margin="2 2 0 0" VerticalContentAlignment="Center"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </telerik:RadDropDownButton.DropDownContent>
            <TextBlock x:Name="FlagEnumEditor_InputTextBlock"
                    Foreground="{Binding (TextElement.Foreground), RelativeSource={RelativeSource AncestorType=ContentPresenter}}"
                    Margin="{TemplateBinding Padding}"
                    Text="{Binding Value, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay, ValidatesOnExceptions=True, ValidatesOnDataErrors=True}"/>
        </telerik:RadDropDownButton>
    </ControlTemplate>
    <Style x:Key="FlagEnumEditorStyle" TargetType="propertyGrid:FlagEnumEditor">
        <Setter Property="Template" Value="{StaticResource FlagEnumEditorNonEditableTemplate}"/>
        <Setter Property="Padding" Value="4 0"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="Validation.ErrorTemplate" Value="{StaticResource ValidationTooltipTemplate}"/>
    </Style>
    <Style TargetType="propertyGrid:FlagEnumEditor" BasedOn="{StaticResource FlagEnumEditorStyle}"/>
</ResourceDictionary>
